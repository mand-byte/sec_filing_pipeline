---
phase: 01
slug: compliant-ingestion-foundation
status: draft
shadcn_initialized: false
preset: none
created: 2026-04-05
---

# Phase 01 — UI Design Contract

> Visual and interaction contract for frontend phases. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

Source notes: design-system state is pre-populated from codebase scan (no React/Next/Vite UI stack, no `components.json`, no Tailwind config, no TSX/CSS UI layer detected). Phase interaction copy is pre-populated from `REQUIREMENTS.md` + `ROADMAP.md` Phase 1 criteria. Remaining visual tokens are explicit defaults for this phase.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none (pre-populated from codebase scan; no shadcn/Tailwind/UI framework detected) |
| Preset | not applicable (default) |
| Component library | none (pre-populated from codebase scan) |
| Icon library | none (default for backend-only phase) |
| Font | Inter, -apple-system, Segoe UI, Roboto, sans-serif (default) |

---

## Spacing Scale

Declared values (must be multiples of 4):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps, inline padding |
| sm | 8px | Compact element spacing |
| md | 16px | Default element spacing |
| lg | 24px | Section padding |
| xl | 32px | Layout gaps |
| 2xl | 48px | Major section breaks |
| 3xl | 64px | Page-level spacing |

Exceptions: icon-only interactive targets must be at least 44x44px if introduced (default accessibility exception).

---

## Typography

| Role | Size | Weight | Line Height |
|------|------|--------|-------------|
| Body | 16px | 400 | 1.5 |
| Label | 14px | 600 | 1.4 |
| Heading | 20px | 600 | 1.2 |
| Display | 28px | 600 | 1.2 |

(Pre-populated as phase default: exactly 4 sizes and 2 weights.)

---

## Color

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | #F8FAFC | Background and primary surfaces |
| Secondary (30%) | #E2E8F0 | Cards, panels, nav/sidebar surfaces |
| Accent (10%) | #2563EB | Primary CTA, active run-state indicator, selected form-family filters, in-app links |
| Destructive | #DC2626 | Destructive actions only |

Accent reserved for: primary run trigger, active selection states, and key navigation links only (not for all interactive elements).

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Primary CTA | Run cold-start ingestion |
| Empty state heading | No ingestion runs yet |
| Empty state body | Start a cold-start ingestion for the configured universe and selected form families. |
| Error state | Ingestion run failed. Review failure reasons and SEC compliance settings, then retry. |
| Destructive confirmation | none in Phase 1 scope: no destructive action is required by ING-01/05/06/OPS-03 |

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none | not applicable (shadcn not initialized) |
| third-party registries | none | not applicable |

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending

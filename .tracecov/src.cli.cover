    1: from dataclasses import asdict
    1: from datetime import date, datetime, timezone
    1: from functools import partial
    1: import json
    1: from pathlib import Path
    1: from typing import Any, cast
       
    1: import typer
    1: from sqlalchemy import func, select
       
    1: from src.config import Settings
    1: from src.db.models import FilingAttempt, PipelineLog
    1: from src.db.repositories import PipelineRepository
    1: from src.db.session import get_session_factory
    1: from src.pipeline.edgar_provider import classify_form_family, fetch_filings_for_security
    1: from src.pipeline.extraction._config import tier2_path
    1: from src.pipeline.extraction.bundles import (
           coerce_numeric_value,
       )
    1: from src.pipeline.extraction.provider import build_bundles_from_provider
    1: from src.pipeline.offline_artifacts import write_run_artifacts
    1: from src.pipeline.offline_evaluator import OfflineEvalSelectors, run_offline_tier2_evaluation
    1: from src.pipeline.golden_10q_numeric_batch import evaluate_10q_numeric_batch
    1: from src.pipeline.route_runtime import (
           BundleBuildOutcome,
           FilingBundle,
           RouteProcessor,
           _normalize_to_utc,
           _safe_write_log,
       )
    1: from src.pipeline.release_gates import evaluate_fix_once_release_gate
    1: from src.pipeline.routers.holding import HoldingRouter
    1: from src.pipeline.routers.issuer import IssuerRouter
    1: from src.pipeline.routers.owner import OwnerRouter
    1: from src.pipeline.review.dashboard import build_review_dashboard_packets, write_review_dashboard
    1: from src.pipeline.review.server import ReviewServerConfig, run_review_server
    1: from src.pipeline.review.workflow import ReviewWorkflowError, ReviewWorkflowService, review_task_detail_asdict
    1: from src.pipeline.scheduler import (
           ROUTE_ORDER,
           build_blocking_scheduler,
           make_run_id,
           ordered_routers,
           run_single_tick,
       )
    1: from src.pipeline.services import PersistenceService
    1: from src.pipeline.types import FilingRecord, RouteName
    1: from src.pipeline.universe import SecurityUniverseRow, load_security_universe
       
       
    1: app = typer.Typer(help="SEC filing pipeline CLI for running phase 1 tasks.")
       
       
    1: def _coerce_numeric_value(value: object) -> float | None:
>>>>>>     return coerce_numeric_value(value)
       
       
    1: def _load_run_once_securities(session: Any, settings: Settings) -> list[SecurityUniverseRow]:
>>>>>>     return load_security_universe(session=session, settings=settings)
       
       
    7: def _build_bundles_from_provider(
           *,
    1:     security: Any,
    1:     route: RouteName,
    1:     start_accepted_at: datetime,
    1:     repo: PipelineRepository,
    1:     run_id: str,
    1: ) -> list[FilingBundle]:
   72:     return build_bundles_from_provider(
   36:         security=security,
   36:         route=route,
   36:         start_accepted_at=start_accepted_at,
   36:         repo=repo,
   36:         run_id=run_id,
   36:         fetch_filings=fetch_filings_for_security,
           )
       
       
    1: def _build_run_artifact_payloads(*, session: Any, run_id: str) -> tuple[dict[str, Any], list[dict[str, Any]], str]:
>>>>>>     route_rows = session.execute(
>>>>>>         select(PipelineLog.route, func.count())
>>>>>>         .where(PipelineLog.run_id == run_id, PipelineLog.stage == "persist", PipelineLog.level == "INFO")
>>>>>>         .group_by(PipelineLog.route)
>>>>>>     ).all()
>>>>>>     route_coverage = {route: int(count) for route, count in route_rows}
       
>>>>>>     error_rows = session.execute(
>>>>>>         select(PipelineLog.error_type, func.count())
>>>>>>         .where(PipelineLog.run_id == run_id, PipelineLog.level == "ERROR")
>>>>>>         .group_by(PipelineLog.error_type)
>>>>>>     ).all()
>>>>>>     error_distribution = {
>>>>>>         (error_type or "UNKNOWN"): int(count)
>>>>>>         for error_type, count in error_rows
           }
       
>>>>>>     filing_attempt_rows = session.scalars(
>>>>>>         select(FilingAttempt)
>>>>>>         .where(FilingAttempt.run_id == run_id)
>>>>>>         .order_by(FilingAttempt.route.asc(), FilingAttempt.accession_no.asc(), FilingAttempt.id.asc())
>>>>>>     ).all()
>>>>>>     filing_attempt_status_counts: dict[str, int] = {}
>>>>>>     filing_attempt_route_counts: dict[str, dict[str, int]] = {}
>>>>>>     for row in filing_attempt_rows:
>>>>>>         filing_attempt_status_counts[row.status] = filing_attempt_status_counts.get(row.status, 0) + 1
>>>>>>         route_status_counts = filing_attempt_route_counts.setdefault(row.route, {})
>>>>>>         route_status_counts[row.status] = route_status_counts.get(row.status, 0) + 1
       
>>>>>>     sample_logs = session.scalars(
>>>>>>         select(PipelineLog)
>>>>>>         .where(PipelineLog.run_id == run_id)
>>>>>>         .order_by(PipelineLog.id.asc())
>>>>>>     ).all()
>>>>>>     samples = [
>>>>>>         {
>>>>>>             "route": row.route,
>>>>>>             "stage": row.stage,
>>>>>>             "level": row.level,
>>>>>>             "message": row.message,
>>>>>>             "cik": row.cik,
>>>>>>             "accession_no": row.accession_no,
>>>>>>             "error_type": row.error_type,
>>>>>>             "error_detail": row.error_detail,
               }
>>>>>>         for row in sample_logs
           ]
       
>>>>>>     summary = {
>>>>>>         "run_id": run_id,
>>>>>>         "coverage": {
>>>>>>             "routes": route_coverage,
>>>>>>             "filing_attempts": {
>>>>>>                 "total": len(filing_attempt_rows),
>>>>>>                 "by_status": filing_attempt_status_counts,
>>>>>>                 "by_route": filing_attempt_route_counts,
                   },
               },
>>>>>>         "errors": {
>>>>>>             "distribution": error_distribution,
               },
>>>>>>         "metrics": {
>>>>>>             "total_logs": len(sample_logs),
>>>>>>             "total_filing_attempts": len(filing_attempt_rows),
               },
           }
       
>>>>>>     diff_markdown = "# Diff\n- baseline comparison unavailable for this run"
>>>>>>     return summary, samples, diff_markdown
       
       
    1: def _parse_route_name(route: str | None) -> RouteName | None:
>>>>>>     if route is None:
>>>>>>         return None
       
>>>>>>     normalized = route.strip().lower()
>>>>>>     if normalized not in ROUTE_ORDER:
>>>>>>         allowed_routes = ", ".join(ROUTE_ORDER)
>>>>>>         raise typer.BadParameter(f"route must be one of: {allowed_routes}")
       
>>>>>>     return cast(RouteName, normalized)
       
       
    1: def _selected_routes(route: RouteName | None) -> tuple[RouteName, ...]:
>>>>>>     if route is None:
>>>>>>         return cast(tuple[RouteName, ...], ROUTE_ORDER)
>>>>>>     return (route,)
       
       
    1: def _run_once_pipeline(*, route: RouteName | None = None) -> None:
>>>>>>     settings = Settings()
>>>>>>     session_factory = get_session_factory(settings)
>>>>>>     selected_routes = _selected_routes(route)
       
>>>>>>     run_id = make_run_id()
       
>>>>>>     with session_factory() as session:
>>>>>>         repo = PipelineRepository(session)
>>>>>>         persistence_service = PersistenceService(session)
>>>>>>         securities = _load_run_once_securities(session, settings)
>>>>>>         processor = RouteProcessor(
>>>>>>             repo=repo,
>>>>>>             persistence_service=persistence_service,
>>>>>>             start_date=settings.start_date,
>>>>>>             provider_bundle_builder=_build_bundles_from_provider,
               )
>>>>>>         router_map = {
>>>>>>             "issuer": IssuerRouter(processor),
>>>>>>             "owner": OwnerRouter(processor),
>>>>>>             "holding": HoldingRouter(processor),
               }
>>>>>>         routers = ordered_routers({route_name: router_map[route_name] for route_name in selected_routes})
>>>>>>         typer.echo("route order: " + " -> ".join(router.name for router in routers))
       
>>>>>>         run_single_tick(
>>>>>>             run_id=run_id,
>>>>>>             securities=securities,
>>>>>>             routers=routers,
>>>>>>             repo=repo,
               )
       
>>>>>>         if settings.write_offline_artifacts:
>>>>>>             summary_payload, sample_payload, diff_markdown = _build_run_artifact_payloads(
>>>>>>                 session=session,
>>>>>>                 run_id=run_id,
                   )
>>>>>>             write_run_artifacts(
>>>>>>                 base_dir=settings.offline_artifacts_dir,
>>>>>>                 run_id=run_id,
>>>>>>                 summary=summary_payload,
>>>>>>                 by_field={},
>>>>>>                 failures=[sample for sample in sample_payload if sample.get("level") == "ERROR"],
>>>>>>                 candidates=sample_payload,
>>>>>>                 diff_markdown=diff_markdown,
                   )
       
       
    2: @app.command("run-once")
    4: def run_once(
    2:     route: str | None = typer.Option(None, "--route", help="Optional route filter: issuer, owner, holding"),
    1: ) -> None:
           """Run the phase-1 pipeline once."""
>>>>>>     _run_once_pipeline(route=_parse_route_name(route))
       
       
    2: @app.command("run-route")
    4: def run_route(
    2:     route: str = typer.Argument(..., help="Route name: issuer, owner, holding"),
    1: ) -> None:
           """Run a single pipeline route once."""
>>>>>>     _run_once_pipeline(route=_parse_route_name(route))
       
       
    2: @app.command("run-issuer")
    2: def run_issuer() -> None:
           """Run the issuer route once."""
>>>>>>     _run_once_pipeline(route="issuer")
       
       
    2: @app.command("run-owner")
    2: def run_owner() -> None:
           """Run the owner route once."""
>>>>>>     _run_once_pipeline(route="owner")
       
       
    2: @app.command("run-holding")
    2: def run_holding() -> None:
           """Run the holding route once."""
>>>>>>     _run_once_pipeline(route="holding")
       
       
   12: def _run_strict_v2_eval(
           *,
    1:     regex_config: str,
    1:     golden_set: str,
    1:     fixtures_dir: str,
    1:     artifacts_dir: str,
    1:     route: str | None,
    1:     form_family: str | None,
    1:     field_name: str | None,
    1:     case_id: str | None,
    1:     baseline: str | None,
    1:     min_pass_rate: float,
    1: ) -> None:
>>>>>>     if not 0.0 <= min_pass_rate <= 1.0:
>>>>>>         raise typer.BadParameter("min_pass_rate must be between 0.0 and 1.0")
       
>>>>>>     result = run_offline_tier2_evaluation(
>>>>>>         regex_config_path=Path(regex_config),
>>>>>>         golden_set_path=Path(golden_set),
>>>>>>         fixtures_dir=Path(fixtures_dir),
>>>>>>         artifacts_dir=Path(artifacts_dir),
>>>>>>         selectors=OfflineEvalSelectors(
>>>>>>             route=route,
>>>>>>             form_family=form_family,
>>>>>>             field_name=field_name,
>>>>>>             case_id=case_id,
               ),
>>>>>>         baseline_path=Path(baseline) if baseline else None,
>>>>>>         min_pass_rate=min_pass_rate,
           )
>>>>>>     typer.echo(f"strict-v2 run_id: {result.run_id}")
>>>>>>     typer.echo(
>>>>>>         "strict-v2 metrics: "
>>>>>>         + f"passed={result.summary['metrics']['passed']} "
>>>>>>         + f"failed={result.summary['metrics']['failed']} "
>>>>>>         + f"gold_strict_accuracy={result.summary['metrics']['gold_strict_accuracy']:.4f} "
>>>>>>         + f"silver_alignment={result.summary['metrics']['silver_alignment']:.4f}"
           )
       
       
    2: @app.command("offline-eval")
   13: def offline_eval(
    3:     regex_config: str = typer.Option(
    1:         str(tier2_path("regex", "default.yaml")),
    1:         "--regex-config",
    1:         help="Path to Tier2 regex config yaml",
           ),
    3:     golden_set: str = typer.Option(
    1:         str(tier2_path("golden_set", "default.yaml")),
    1:         "--golden-set",
    1:         help="Path to Tier2 golden set yaml",
           ),
    3:     fixtures_dir: str = typer.Option(
    1:         str(tier2_path("fixtures")),
    1:         "--fixtures-dir",
    1:         help="Directory of local fixture snapshots",
           ),
    3:     artifacts_dir: str = typer.Option(
    1:         "artifacts/offline",
    1:         "--artifacts-dir",
    1:         help="Directory for offline evaluator artifacts",
           ),
    2:     route: str | None = typer.Option(None, "--route", help="Optional route filter"),
    2:     form_family: str | None = typer.Option(None, "--form-family", help="Optional form family filter"),
    2:     field_name: str | None = typer.Option(None, "--field", help="Optional field filter"),
    2:     case_id: str | None = typer.Option(None, "--case-id", help="Optional case id filter"),
    2:     baseline: str | None = typer.Option(None, "--baseline", help="Optional baseline summary json"),
    2:     min_pass_rate: float = typer.Option(0.95, "--min-pass-rate", min=0.0, max=1.0, help="Minimum pass rate threshold"),
    1: ) -> None:
           """Run Tier2 offline evaluator using local fixtures and golden set."""
>>>>>>     _run_strict_v2_eval(
>>>>>>         regex_config=regex_config,
>>>>>>         golden_set=golden_set,
>>>>>>         fixtures_dir=fixtures_dir,
>>>>>>         artifacts_dir=artifacts_dir,
>>>>>>         route=route,
>>>>>>         form_family=form_family,
>>>>>>         field_name=field_name,
>>>>>>         case_id=case_id,
>>>>>>         baseline=baseline,
>>>>>>         min_pass_rate=min_pass_rate,
           )
       
       
    2: @app.command("strict-v2-eval")
   13: def strict_v2_eval(
    3:     regex_config: str = typer.Option(
    1:         str(tier2_path("regex", "default.yaml")),
    1:         "--regex-config",
    1:         help="Path to strict-v2 regex config yaml",
           ),
    3:     golden_set: str = typer.Option(
    1:         str(tier2_path("golden_set", "default.yaml")),
    1:         "--golden-set",
    1:         help="Path to strict-v2 golden set yaml",
           ),
    3:     fixtures_dir: str = typer.Option(
    1:         str(tier2_path("fixtures")),
    1:         "--fixtures-dir",
    1:         help="Directory of local strict-v2 fixture snapshots",
           ),
    3:     artifacts_dir: str = typer.Option(
    1:         "artifacts/golden",
    1:         "--artifacts-dir",
    1:         help="Directory for strict-v2 artifacts",
           ),
    2:     route: str | None = typer.Option(None, "--route", help="Optional route filter"),
    2:     form_family: str | None = typer.Option(None, "--form-family", help="Optional form family filter"),
    2:     field_name: str | None = typer.Option(None, "--field", help="Optional field filter"),
    2:     case_id: str | None = typer.Option(None, "--case-id", help="Optional case id filter"),
    2:     baseline: str | None = typer.Option(None, "--baseline", help="Optional baseline summary json"),
    2:     min_pass_rate: float = typer.Option(0.95, "--min-pass-rate", min=0.0, max=1.0, help="Minimum pass rate threshold"),
    1: ) -> None:
           """Run strict-v2 evaluation and emit the golden artifact contract."""
>>>>>>     _run_strict_v2_eval(
>>>>>>         regex_config=regex_config,
>>>>>>         golden_set=golden_set,
>>>>>>         fixtures_dir=fixtures_dir,
>>>>>>         artifacts_dir=artifacts_dir,
>>>>>>         route=route,
>>>>>>         form_family=form_family,
>>>>>>         field_name=field_name,
>>>>>>         case_id=case_id,
>>>>>>         baseline=baseline,
>>>>>>         min_pass_rate=min_pass_rate,
           )
       
       
    2: @app.command("golden-10q-numeric-batch")
    5: def golden_10q_numeric_batch(
    3:     golden_path: str = typer.Option(
    1:         ...,
    1:         "--golden-path",
    1:         help="Path to the adjudicated 10-Q numeric batch golden set",
           ),
    3:     snapshot_dir: str = typer.Option(
    1:         ...,
    1:         "--snapshot-dir",
    1:         help="Directory containing batch_001 candidate snapshots",
           ),
    1: ) -> None:
>>>>>>     result = evaluate_10q_numeric_batch(
>>>>>>         golden_path=Path(golden_path),
>>>>>>         snapshot_dir=Path(snapshot_dir),
           )
>>>>>>     metrics = result.summary["metrics"]
>>>>>>     typer.echo(
>>>>>>         "golden metrics: "
>>>>>>         + f"batches={metrics['total_batches']} "
>>>>>>         + f"field_checks={metrics['total_field_checks']} "
>>>>>>         + f"candidate_recall={metrics['candidate_recall']} "
>>>>>>         + f"top1_accuracy={metrics['top1_accuracy']} "
>>>>>>         + f"batch_all_match_rate={metrics['batch_all_match_rate']}"
           )
>>>>>>     for field_name, field_metrics in result.by_field.items():
>>>>>>         typer.echo(
>>>>>>             "field "
>>>>>>             + f"{field_name}: "
>>>>>>             + f"candidate_recall={field_metrics['candidate_recall']} "
>>>>>>             + f"top1_accuracy={field_metrics['top1_accuracy']}"
               )
       
       
    2: @app.command("review-list")
    6: def review_list(
    2:     status: str = typer.Option("open", "--status", help="Review task status filter"),
    2:     route: str | None = typer.Option(None, "--route", help="Optional route filter"),
    2:     limit: int = typer.Option(100, "--limit", min=1, max=1000, help="Maximum tasks to return"),
    1: ) -> None:
>>>>>>     settings = Settings()
>>>>>>     with get_session_factory(settings)() as session:
>>>>>>         service = ReviewWorkflowService(session)
>>>>>>         payload = [
>>>>>>             {
>>>>>>                 **asdict(task),
>>>>>>                 "created_at": task.created_at.isoformat(),
                   }
>>>>>>             for task in service.list_tasks(status=status, route=route, limit=limit)
               ]
>>>>>>     typer.echo(json.dumps(payload, ensure_ascii=False, indent=2))
       
       
    2: @app.command("review-show")
    4: def review_show(
    2:     task_id: int = typer.Argument(..., help="Review task id"),
    1: ) -> None:
>>>>>>     settings = Settings()
>>>>>>     with get_session_factory(settings)() as session:
>>>>>>         service = ReviewWorkflowService(session)
>>>>>>         try:
>>>>>>             detail = service.get_task_detail(task_id=task_id)
>>>>>>         except ReviewWorkflowError as exc:
>>>>>>             raise typer.BadParameter(str(exc)) from exc
>>>>>>     typer.echo(json.dumps(review_task_detail_asdict(detail), ensure_ascii=False, indent=2))
       
       
    2: @app.command("review-assign")
    5: def review_assign(
    2:     task_id: int = typer.Argument(..., help="Review task id"),
    2:     assignee: str = typer.Argument(..., help="Assignee name"),
    1: ) -> None:
>>>>>>     settings = Settings()
>>>>>>     with get_session_factory(settings)() as session:
>>>>>>         service = ReviewWorkflowService(session)
>>>>>>         try:
>>>>>>             summary = service.assign_task(task_id=task_id, assignee=assignee)
>>>>>>         except ReviewWorkflowError as exc:
>>>>>>             raise typer.BadParameter(str(exc)) from exc
>>>>>>     typer.echo(
>>>>>>         json.dumps(
>>>>>>             {
>>>>>>                 "task_id": summary.task_id,
>>>>>>                 "status": summary.status,
>>>>>>                 "assignee": summary.assignee,
                   },
>>>>>>             ensure_ascii=False,
>>>>>>             indent=2,
               )
           )
       
       
    2: @app.command("review-resolve")
    9: def review_resolve(
    2:     task_id: int = typer.Argument(..., help="Review task id"),
    2:     decision: str = typer.Option(..., "--decision", help="accept, corrected, reject, not_applicable"),
    2:     reviewer: str = typer.Option(..., "--reviewer", help="Reviewer name"),
    2:     error_code: str | None = typer.Option(None, "--error-code", help="Normalized extraction failure class for non-accept decisions"),
    2:     comment: str | None = typer.Option(None, "--comment", help="Optional review comment"),
    2:     corrected_json: str | None = typer.Option(None, "--corrected-json", help="JSON payload for corrected decisions"),
    1: ) -> None:
>>>>>>     settings = Settings()
>>>>>>     with get_session_factory(settings)() as session:
>>>>>>         service = ReviewWorkflowService(session)
>>>>>>         try:
>>>>>>             summary = service.resolve_task(
>>>>>>                 task_id=task_id,
>>>>>>                 decision=decision,
>>>>>>                 reviewer=reviewer,
>>>>>>                 error_code=error_code,
>>>>>>                 comment=comment,
>>>>>>                 corrected_json=corrected_json,
                   )
>>>>>>         except ReviewWorkflowError as exc:
>>>>>>             raise typer.BadParameter(str(exc)) from exc
>>>>>>     typer.echo(
>>>>>>         json.dumps(
>>>>>>             {
>>>>>>                 "task_id": summary.task_id,
>>>>>>                 "status": summary.status,
>>>>>>                 "assignee": summary.assignee,
                   },
>>>>>>             ensure_ascii=False,
>>>>>>             indent=2,
               )
           )
       
       
    2: @app.command("review-dashboard")
    7: def review_dashboard(
    2:     status: str = typer.Option("open", "--status", help="Review task status filter"),
    2:     route: str | None = typer.Option(None, "--route", help="Optional route filter"),
    2:     limit: int = typer.Option(100, "--limit", min=1, max=1000, help="Maximum tasks to export"),
    3:     output_dir: Path = typer.Option(
    1:         Path("artifacts/review_dashboard"),
    1:         "--output-dir",
    1:         help="Directory for the exported review dashboard",
           ),
    1: ) -> None:
>>>>>>     settings = Settings()
>>>>>>     with get_session_factory(settings)() as session:
>>>>>>         service = ReviewWorkflowService(session)
>>>>>>         packets = build_review_dashboard_packets(
>>>>>>             service=service,
>>>>>>             status=status,
>>>>>>             route=route,
>>>>>>             limit=limit,
               )
>>>>>>     dashboard_dir = write_review_dashboard(output_dir=output_dir, packets=packets)
>>>>>>     typer.echo(str(dashboard_dir))
       
       
    2: @app.command("review-serve")
    8: def review_serve(
    2:     status: str = typer.Option("open", "--status", help="Review task status filter"),
    2:     route: str | None = typer.Option(None, "--route", help="Optional route filter"),
    2:     limit: int = typer.Option(100, "--limit", min=1, max=1000, help="Maximum tasks to expose"),
    2:     host: str = typer.Option("127.0.0.1", "--host", help="Host interface to bind"),
    2:     port: int = typer.Option(8765, "--port", min=1, max=65535, help="Port to bind"),
    1: ) -> None:
>>>>>>     settings = Settings()
>>>>>>     session_factory = get_session_factory(settings)
>>>>>>     typer.echo(f"http://{host}:{port}")
>>>>>>     run_review_server(
>>>>>>         session_factory=session_factory,
>>>>>>         config=ReviewServerConfig(
>>>>>>             host=host,
>>>>>>             port=port,
>>>>>>             status=status,
>>>>>>             route=route,
>>>>>>             limit=limit,
               ),
           )
       
       
    2: @app.command("release-gate")
    4: def release_gate(
    3:     output_dir: Path = typer.Option(
    1:         Path("artifacts/release_gate"),
    1:         "--output-dir",
    1:         help="Directory for exported fix-once regression packets and summary",
           ),
    1: ) -> None:
>>>>>>     settings = Settings()
>>>>>>     with get_session_factory(settings)() as session:
>>>>>>         result = evaluate_fix_once_release_gate(session=session, output_dir=output_dir)
>>>>>>     typer.echo(json.dumps(asdict(result), ensure_ascii=False, indent=2))
>>>>>>     if not result.passed:
>>>>>>         raise typer.Exit(code=1)
       
       
    2: @app.command("schedule")
    4: def schedule(
    2:     route: str | None = typer.Option(None, "--route", help="Optional route filter: issuer, owner, holding"),
    1: ) -> None:
           """Run the phase-1 scheduler loop."""
>>>>>>     settings = Settings()
>>>>>>     parsed_route = _parse_route_name(route)
>>>>>>     scheduler = build_blocking_scheduler(
>>>>>>         interval_minutes=settings.scheduler_interval_minutes,
>>>>>>         tick_callable=partial(_run_once_pipeline, route=parsed_route),
           )
>>>>>>     scheduler.start()
       
       
    1: if __name__ == "__main__":
>>>>>>     app()

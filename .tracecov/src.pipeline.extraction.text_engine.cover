    1: from __future__ import annotations
       
    1: import json
    1: import re
    1: from collections.abc import Mapping
    1: from typing import Literal, TypedDict
       
    1: from src.pipeline.extraction.text_contracts import SpanPolicy, TextFieldSpec, TextLocatorKind
    1: from src.pipeline.extraction.text_locators import run_text_locator
       
       
    2: class TextExtractionOk(TypedDict):
    1:     status: Literal["ok"]
    1:     value_text: str
    1:     value_json: None
    1:     locator_kind: TextLocatorKind
    1:     locator_path: str
    1:     source_span: str
    1:     source_section: str | None
    1:     source_item_no: str | None
    1:     source_locator_json: str
    1:     source_heading_path_json: str
    1:     source_block_offsets_json: str
    1:     adequacy_signals_json: str
    1:     retry_history_json: str
    1:     selection_trace_json: str
       
       
    2: class TextExtractionFailure(TypedDict):
    1:     status: Literal["error"]
    1:     error_code: str
       
       
    1: TextExtractionOutcome = TextExtractionOk | TextExtractionFailure
       
       
    2: class TextExtractionEngine:
    1:     def _heading_path_json(self, *, window_hit: Mapping[str, object]) -> str:
   40:         locator_path = str(window_hit["locator_path"])
   40:         heading = locator_path
   40:         if "[" in locator_path and locator_path.endswith("]"):
   40:             heading = locator_path.split("[", 1)[1][:-1]
   40:         return json.dumps([heading], ensure_ascii=False)
       
    7:     def _block_offsets_json(
               self,
               *,
    1:         window_hit: Mapping[str, object],
    1:         span_start: int,
    1:         span_end: int,
    1:         source_start: int,
    1:         source_end: int,
    1:     ) -> str:
   40:         payload = {
   40:             "window_base": int(window_hit["window_base"]),
   40:             "span_start_in_window": span_start,
   40:             "span_end_in_window": span_end,
   40:             "source_start": source_start,
   40:             "source_end": source_end,
               }
   40:         if "item_body_offset" in window_hit and isinstance(window_hit.get("item_body_offset"), int):
>>>>>>             payload["item_body_offset"] = int(window_hit["item_body_offset"])
   40:         if "section_body_offset" in window_hit and isinstance(window_hit.get("section_body_offset"), int):
>>>>>>             payload["section_body_offset"] = int(window_hit["section_body_offset"])
   40:         return json.dumps(payload, ensure_ascii=False, sort_keys=True)
       
    7:     def _adequacy_signals_json(
               self,
               *,
    1:         field_spec: TextFieldSpec,
    1:         window_text: str,
    1:         pattern: str,
    1:         distinct_match_count: int,
    1:         value_text: str,
    1:     ) -> str:
   40:         window_token_count = len(re.findall(r"\S+", window_text))
   40:         value_token_count = len(re.findall(r"\S+", value_text))
   40:         preferred_tokens_match = None
   40:         if field_spec.span_policy is not None and field_spec.span_policy.preferred_tokens is not None:
   40:             minimum, maximum = field_spec.span_policy.preferred_tokens
   40:             preferred_tokens_match = minimum <= window_token_count <= maximum
   40:         payload = {
   40:             "window_found": True,
   40:             "pattern": pattern,
   40:             "distinct_match_count": distinct_match_count,
   40:             "window_token_count": window_token_count,
   40:             "value_token_count": value_token_count,
   40:             "value_length": len(value_text),
   40:             "span_policy_applied": field_spec.span_policy is not None,
   40:             "preferred_tokens_match": preferred_tokens_match,
               }
   40:         return json.dumps(payload, ensure_ascii=False, sort_keys=True)
       
    1:     def _span_policy_error(self, *, window_text: str, span_policy: SpanPolicy) -> str | None:
   40:         lowered_window = window_text.casefold()
   40:         header_text = window_text.splitlines()[0].strip().casefold() if window_text.splitlines() else ""
       
   40:         if span_policy.anchor_headers:
   97:             if not any(anchor.casefold() in header_text for anchor in span_policy.anchor_headers):
>>>>>>                 return "SPAN_POLICY_FAILED"
       
   40:         token_count = len(re.findall(r"\S+", window_text))
   40:         min_tokens = span_policy.min_tokens
   40:         max_tokens = span_policy.max_tokens
   40:         if span_policy.preferred_tokens is not None:
   40:             preferred_min, preferred_max = span_policy.preferred_tokens
   40:             min_tokens = max(min_tokens, preferred_min)
   40:             max_tokens = preferred_max if max_tokens == 0 else min(max_tokens, preferred_max)
       
   40:         if min_tokens > 0 and token_count < min_tokens:
>>>>>>             return "SPAN_POLICY_FAILED"
   40:         if max_tokens > 0 and token_count > max_tokens:
>>>>>>             return "SPAN_POLICY_FAILED"
       
   40:         if span_policy.must_include:
>>>>>>             if any(required.casefold() not in lowered_window for required in span_policy.must_include):
>>>>>>                 return "SPAN_POLICY_FAILED"
       
   40:         if span_policy.avoid:
  120:             if any(avoid.casefold() in lowered_window for avoid in span_policy.avoid):
>>>>>>                 return "SPAN_POLICY_FAILED"
       
   40:         return None
       
    1:     def _qa_check(self, value_text: str, qa_rules: Mapping[str, int | float | bool]) -> str | None:
   40:         min_len = qa_rules.get("min_len")
   40:         if isinstance(min_len, (int, float)) and not isinstance(min_len, bool) and len(value_text) < int(min_len):
>>>>>>             return "QA_FAILED"
       
   40:         max_len = qa_rules.get("max_len")
   40:         if isinstance(max_len, (int, float)) and not isinstance(max_len, bool) and len(value_text) > int(max_len):
>>>>>>             return "QA_FAILED"
       
   40:         return None
       
    1:     def extract_field(self, *, filing: object, field_spec: TextFieldSpec) -> TextExtractionOutcome:
   40:         if field_spec.output_kind != "text":
>>>>>>             return {"status": "error", "error_code": "NORMALIZATION_FAILED"}
       
   40:         best_failure: str = "PATTERN_NOT_MATCHED"
   40:         failure_priority = {
   40:             "PATTERN_NOT_MATCHED": 0,
   40:             "MULTIPLE_CANDIDATES": 1,
   40:             "QA_FAILED": 2,
   40:             "SPAN_POLICY_FAILED": 3,
               }
   40:         saw_window = False
       
   40:         for locator in field_spec.locators:
   40:             window_hit = run_text_locator(filing=filing, locator=locator, anchors=field_spec.anchor_terms)
   40:             if window_hit is None:
>>>>>>                 continue
       
   40:             saw_window = True
   40:             window_text = window_hit["value"]
   40:             if field_spec.span_policy is not None:
   80:                 span_policy_error = self._span_policy_error(
   40:                     window_text=window_text,
   40:                     span_policy=field_spec.span_policy,
                       )
   40:                 if span_policy_error is not None:
>>>>>>                     if failure_priority[span_policy_error] > failure_priority[best_failure]:
>>>>>>                         best_failure = span_policy_error
>>>>>>                     continue
       
   40:             for pattern in field_spec.regex_patterns:
   40:                 matches = list(re.finditer(pattern, window_text))
   40:                 if not matches:
>>>>>>                     continue
       
   40:                 distinct_matches: dict[str, tuple[str, re.Match[str]]] = {}
   80:                 for match in matches:
   40:                     group_index = 1 if match.lastindex else 0
   40:                     extracted = match.group(group_index).strip()
   40:                     normalized_extracted = extracted.casefold()
   40:                     if extracted and normalized_extracted not in distinct_matches:
   40:                         distinct_matches[normalized_extracted] = (extracted, match)
       
   40:                 if not distinct_matches:
>>>>>>                     continue
       
   40:                 if len(distinct_matches) > 1:
>>>>>>                     if failure_priority["MULTIPLE_CANDIDATES"] > failure_priority[best_failure]:
>>>>>>                         best_failure = "MULTIPLE_CANDIDATES"
>>>>>>                     continue
       
   40:                 value_text, source_match = next(iter(distinct_matches.values()))
   40:                 qa_error = self._qa_check(value_text, field_spec.qa_rules)
   40:                 if qa_error is not None:
>>>>>>                     if failure_priority[qa_error] > failure_priority[best_failure]:
>>>>>>                         best_failure = qa_error
>>>>>>                     continue
       
   40:                 source_group = 1 if source_match.lastindex else 0
   40:                 span_start, span_end = source_match.span(source_group)
   40:                 source_start = window_hit["window_base"] + span_start
   40:                 source_end = window_hit["window_base"] + span_end
       
   40:                 source_span = f"{source_start}:{source_end}"
   40:                 if window_hit["locator_kind"] == "item_window":
>>>>>>                     item_body_offset = window_hit.get("item_body_offset")
>>>>>>                     if isinstance(item_body_offset, int):
>>>>>>                         if span_end <= item_body_offset:
>>>>>>                             source_span = f"header:{span_start}:{span_end}"
                               else:
>>>>>>                             body_start = max(0, span_start - item_body_offset)
>>>>>>                             body_end = max(0, span_end - item_body_offset)
>>>>>>                             source_span = f"{body_start}:{body_end}"
   40:                 elif window_hit["locator_kind"] == "section_window":
   40:                     section_body_offset = window_hit.get("section_body_offset")
   40:                     if isinstance(section_body_offset, int):
>>>>>>                         if span_end <= section_body_offset:
>>>>>>                             source_span = f"header:{span_start}:{span_end}"
                               else:
>>>>>>                             body_start = max(0, span_start - section_body_offset)
>>>>>>                             body_end = max(0, span_end - section_body_offset)
>>>>>>                             source_span = f"{body_start}:{body_end}"
       
   40:                 return {
   40:                     "status": "ok",
   40:                     "value_text": value_text,
   40:                     "value_json": None,
   40:                     "locator_kind": window_hit["locator_kind"],
   40:                     "locator_path": window_hit["locator_path"],
   40:                     "source_span": source_span,
   40:                     "source_section": window_hit.get("source_section"),
   40:                     "source_item_no": window_hit.get("source_item_no"),
   80:                     "source_locator_json": json.dumps(
   40:                         {
   40:                             "locator_kind": window_hit["locator_kind"],
   40:                             "locator_path": window_hit["locator_path"],
   40:                             "source_section": window_hit.get("source_section"),
   40:                             "source_item_no": window_hit.get("source_item_no"),
   40:                             "source_span": source_span,
                               },
   40:                         ensure_ascii=False,
   40:                         sort_keys=True,
                           ),
   40:                     "source_heading_path_json": self._heading_path_json(window_hit=window_hit),
   80:                     "source_block_offsets_json": self._block_offsets_json(
   40:                         window_hit=window_hit,
   40:                         span_start=span_start,
   40:                         span_end=span_end,
   40:                         source_start=source_start,
   40:                         source_end=source_end,
                           ),
   80:                     "adequacy_signals_json": self._adequacy_signals_json(
   40:                         field_spec=field_spec,
   40:                         window_text=window_text,
   40:                         pattern=pattern,
   40:                         distinct_match_count=len(distinct_matches),
   40:                         value_text=value_text,
                           ),
   40:                     "retry_history_json": "[]",
   80:                     "selection_trace_json": json.dumps(
   40:                         {
   40:                             "pattern": pattern,
   40:                             "distinct_match_count": len(distinct_matches),
   40:                             "selected_value": value_text,
   40:                             "locator_kind": window_hit["locator_kind"],
   40:                             "locator_path": window_hit["locator_path"],
                               },
   40:                         ensure_ascii=False,
   40:                         sort_keys=True,
                           ),
                       }
       
>>>>>>         if not saw_window:
>>>>>>             return {"status": "error", "error_code": "WINDOW_NOT_FOUND"}
       
>>>>>>         return {"status": "error", "error_code": best_failure}

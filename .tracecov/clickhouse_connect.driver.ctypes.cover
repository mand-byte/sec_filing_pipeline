    1: import logging
    1: import os
       
    1: import clickhouse_connect.driver.dataconv as pydc
    1: import clickhouse_connect.driver.npconv as pync
    1: from clickhouse_connect.driver.buffer import ResponseBuffer
    1: from clickhouse_connect.driver.common import coerce_bool
       
    1: logger = logging.getLogger(__name__)
       
    1: RespBuffCls = ResponseBuffer
    1: data_conv = pydc
       # numpy_conv is resolved lazily via __getattr__ to avoid eagerly importing numpy
       
       
       # pylint: disable=import-outside-toplevel,global-statement
       
    1: def connect_c_modules():
    1:     if not coerce_bool(os.environ.get('CLICKHOUSE_CONNECT_USE_C', True)):
>>>>>>         logger.info('ClickHouse Connect C optimizations disabled')
>>>>>>         return
       
           global RespBuffCls, data_conv
    1:     try:
    1:         from clickhouse_connect.driverc.buffer import ResponseBuffer as CResponseBuffer
    1:         import clickhouse_connect.driverc.dataconv as cdc
       
    1:         data_conv = cdc
    1:         RespBuffCls = CResponseBuffer
    1:         logger.debug('Successfully imported ClickHouse Connect C data optimizations')
>>>>>>     except ImportError as ex:
>>>>>>         logger.warning('Unable to connect optimized C data functions [%s], falling back to pure Python',
>>>>>>                        str(ex))
       
       
    1: def _resolve_numpy_conv():
>>>>>>     if "numpy_conv" in globals():
>>>>>>         return
>>>>>>     if coerce_bool(os.environ.get('CLICKHOUSE_CONNECT_USE_C', True)):
>>>>>>         try:
>>>>>>             import clickhouse_connect.driverc.npconv as cnc
>>>>>>             globals()["numpy_conv"] = cnc
>>>>>>             logger.debug('Successfully import ClickHouse Connect C/Numpy optimizations')
>>>>>>             return
>>>>>>         except ImportError as ex:
>>>>>>             logger.debug('Unable to connect ClickHouse Connect C to Numpy API [%s], falling back to pure Python',
>>>>>>                          str(ex))
>>>>>>     globals()["numpy_conv"] = pync
       
       
    1: def __getattr__(name):
   10:     if name == "numpy_conv":
>>>>>>         _resolve_numpy_conv()
>>>>>>         return globals()["numpy_conv"]
   10:     raise AttributeError(f"module {__name__!r} has no attribute {name!r}")
       
       
    1: connect_c_modules()

>>>>>> from __future__ import annotations
       
>>>>>> from collections.abc import Mapping
>>>>>> import functools
>>>>>> from pathlib import Path
>>>>>> from typing import Any
>>>>>> import warnings
       
>>>>>> import pluggy
       
>>>>>> from ..compat import LEGACY_PATH
>>>>>> from ..compat import legacy_path
>>>>>> from ..deprecated import HOOK_LEGACY_PATH_ARG
       
       
       # hookname: (Path, LEGACY_PATH)
>>>>>> imply_paths_hooks: Mapping[str, tuple[str, str]] = {
>>>>>>     "pytest_ignore_collect": ("collection_path", "path"),
>>>>>>     "pytest_collect_file": ("file_path", "path"),
>>>>>>     "pytest_pycollect_makemodule": ("module_path", "path"),
>>>>>>     "pytest_report_header": ("start_path", "startdir"),
>>>>>>     "pytest_report_collectionfinish": ("start_path", "startdir"),
       }
       
       
>>>>>> def _check_path(path: Path, fspath: LEGACY_PATH) -> None:
>>>>>>     if Path(fspath) != path:
>>>>>>         raise ValueError(
>>>>>>             f"Path({fspath!r}) != {path!r}\n"
                   "if both path and fspath are given they need to be equal"
               )
       
       
>>>>>> class PathAwareHookProxy:
           """
           this helper wraps around hook callers
           until pluggy supports fixingcalls, this one will do
       
           it currently doesn't return full hook caller proxies for fixed hooks,
           this may have to be changed later depending on bugs
           """
       
>>>>>>     def __init__(self, hook_relay: pluggy.HookRelay) -> None:
    1:         self._hook_relay = hook_relay
       
>>>>>>     def __dir__(self) -> list[str]:
>>>>>>         return dir(self._hook_relay)
       
>>>>>>     def __getattr__(self, key: str) -> pluggy.HookCaller:
   37:         hook: pluggy.HookCaller = getattr(self._hook_relay, key)
   37:         if key not in imply_paths_hooks:
   33:             self.__dict__[key] = hook
   33:             return hook
               else:
    4:             path_var, fspath_var = imply_paths_hooks[key]
       
    8:             @functools.wraps(hook)
    8:             def fixed_hook(**kw: Any) -> Any:
  153:                 path_value: Path | None = kw.pop(path_var, None)
  153:                 fspath_value: LEGACY_PATH | None = kw.pop(fspath_var, None)
  153:                 if fspath_value is not None:
>>>>>>                     warnings.warn(
>>>>>>                         HOOK_LEGACY_PATH_ARG.format(
>>>>>>                             pylib_path_arg=fspath_var, pathlib_path_arg=path_var
                               ),
>>>>>>                         stacklevel=2,
                           )
  153:                 if path_value is not None:
  153:                     if fspath_value is not None:
>>>>>>                         _check_path(path_value, fspath_value)
                           else:
  153:                         fspath_value = legacy_path(path_value)
                       else:
>>>>>>                     assert fspath_value is not None
>>>>>>                     path_value = Path(fspath_value)
       
  153:                 kw[path_var] = path_value
  153:                 kw[fspath_var] = fspath_value
  153:                 return hook(**kw)
       
    4:             fixed_hook.name = hook.name  # type: ignore[attr-defined]
    4:             fixed_hook.spec = hook.spec  # type: ignore[attr-defined]
    4:             fixed_hook.__name__ = key
    4:             self.__dict__[key] = fixed_hook
    4:             return fixed_hook  # type: ignore[return-value]

    1: from __future__ import annotations
       
    1: from datetime import datetime, timezone
    1: from decimal import Decimal
    1: from types import SimpleNamespace
       
    1: import pandas as pd
    1: import src.cli as cli_module
    1: import src.pipeline.extraction.provider as provider_module
    1: from src.pipeline.edgar_provider import FilingEnvelope
    1: from src.pipeline.route_runtime import BundleBuildOutcome, FilingBundle
    1: from src.pipeline.services import EvidenceInput, FactInput
       
       
    2: class FakeSummaryPage:
    1:     def __init__(self, *, other_included_managers_count: int, total_holdings: int, total_value: Decimal):
   10:         self.other_included_managers_count = other_included_managers_count
   10:         self.total_holdings = total_holdings
   10:         self.total_value = total_value
       
       
    2: class FakePrimaryFormInformation:
    1:     def __init__(self, *, summary_page: FakeSummaryPage):
   10:         self.summary_page = summary_page
       
       
    2: class Fake13F:
    7:     def __init__(
               self,
               *,
    1:         form: str,
    1:         rows: list[dict[str, object]],
    2:         other_included_managers_count: int = 0,
    2:         total_holdings: int | None = None,
    2:         total_value: Decimal | None = None,
    2:         sections: list[str] | None = None,
           ):
   10:         self.form = form
   10:         self.infotable = pd.DataFrame(rows)
   20:         self.primary_form_information = FakePrimaryFormInformation(
   20:             summary_page=FakeSummaryPage(
   10:                 other_included_managers_count=other_included_managers_count,
   10:                 total_holdings=total_holdings if total_holdings is not None else len(rows),
   16:                 total_value=total_value if total_value is not None else Decimal(str(sum(float(row["Value"]) for row in rows))),
                   )
               )
   10:         self.total_holdings = total_holdings if total_holdings is not None else len(rows)
   16:         self.total_value = total_value if total_value is not None else Decimal(str(sum(float(row["Value"]) for row in rows)))
   10:         self._sections = sections or []
       
    1:     def sections(self) -> list[str]:
    8:         return self._sections
       
       
    2: class Fake13FFiling:
    1:     def __init__(self, report: Fake13F):
   10:         self.form = report.form
   10:         self._report = report
       
    1:     def obj(self) -> Fake13F:
    4:         return self._report
       
    1:     def sections(self) -> list[str]:
    8:         return self._report.sections()
       
       
    2: class FakeRepo:
    1:     def __init__(self) -> None:
   10:         self.logs: list[dict[str, object]] = []
       
    1:     def write_log(self, **kwargs: object) -> None:
    8:         self.logs.append(dict(kwargs))
       
       
    1: def test_build_bundles_from_provider_emits_13f_position_rows(monkeypatch) -> None:
    2:     envelope = FilingEnvelope(
    1:         accession_no="0000000000-24-000200",
    1:         cik="0001067983",
    1:         form_type="13F-HR",
    1:         accepted_at=datetime(2024, 5, 15, tzinfo=timezone.utc),
    2:         filing=Fake13FFiling(
    2:             Fake13F(
    1:                 form="13F-HR",
    1:                 rows=[
    1:                     {
    1:                         "Issuer": "MICROSOFT CORP",
    1:                         "Class": "COM",
    1:                         "Cusip": "594918104",
    1:                         "Value": 1250,
    1:                         "SharesPrnAmount": 10000,
    1:                         "SoleVoting": 9000,
    1:                         "SharedVoting": 500,
    1:                         "NonVoting": 500,
                           },
    1:                     {
    1:                         "Issuer": "APPLE INC",
    1:                         "Class": "COM",
    1:                         "Cusip": "037833100",
    1:                         "Value": 750,
    1:                         "SharesPrnAmount": 4000,
    1:                         "SoleVoting": 3000,
    1:                         "SharedVoting": 500,
    1:                         "NonVoting": 500,
                           },
                       ],
    1:                 other_included_managers_count=2,
    1:                 total_holdings=2,
    1:                 total_value=Decimal("2000"),
    1:                 sections=["Cover page\nThis is a holdings report filed by the manager."],
                   )
               ),
           )
       
    2:     monkeypatch.setattr(
    1:         cli_module,
    1:         "fetch_filings_for_security",
    2:         lambda *, security, route, start_accepted_at: [envelope],
           )
       
    1:     repo = FakeRepo()
    1:     security = SimpleNamespace(cik="0001067983", ticker="BRK")
    2:     bundles = cli_module._build_bundles_from_provider(
    1:         security=security,
    1:         route="holding",
    1:         start_accepted_at=datetime(2024, 4, 1, tzinfo=timezone.utc),
    1:         repo=repo,
    1:         run_id="run-holding-1",
           )
       
    1:     assert len(bundles) == 1
    1:     bundle = bundles[0]
   15:     facts = {(fact.field_name, fact.subject_key): fact.value_numeric for fact in bundle.facts}
    1:     assert facts[("position_value_usd", "position:1")] == 1250000.0
    1:     assert facts[("position_value_usd", "position:2")] == 750000.0
    1:     assert facts[("shares_or_principal_amount", "position:1")] == 10000.0
    1:     assert facts[("sole_voting_auth_shares", "position:1")] == 9000.0
    1:     assert facts[("shared_voting_auth_shares", "position:2")] == 500.0
    1:     assert facts[("none_voting_auth_shares", "position:2")] == 500.0
    1:     assert facts[("other_included_managers_count", "document")] == 2.0
    1:     assert facts[("info_table_entry_total", "document")] == 2.0
    1:     assert facts[("info_table_value_total_usd", "document")] == 2000000.0
   15:     text_facts = {(fact.field_name, fact.subject_key): fact.value_text for fact in bundle.facts if fact.value_text is not None}
    1:     assert text_facts[("manager_structure_quant", "document")] == "holdings"
       
   15:     evidence = {(item.field_name, item.subject_key): item for item in bundle.evidences}
    1:     assert evidence[("position_value_usd", "position:1")].source_span == "infotable[0].Value"
    1:     assert evidence[("info_table_entry_total", "document")].source_span == "summary_page.tableEntryTotal"
    1:     assert evidence[("info_table_value_total_usd", "document")].normalized_value == "2000000.0"
    1:     assert repo.logs == []
       
       
    1: def test_build_bundles_from_provider_keeps_13f_numeric_when_text_field_extraction_fails(monkeypatch) -> None:
    2:     envelope = FilingEnvelope(
    1:         accession_no="0000000000-24-000200D",
    1:         cik="0001067983",
    1:         form_type="13F-HR",
    1:         accepted_at=datetime(2024, 5, 15, tzinfo=timezone.utc),
    2:         filing=Fake13FFiling(
    2:             Fake13F(
    1:                 form="13F-HR",
    1:                 rows=[
    1:                     {
    1:                         "Issuer": "MICROSOFT CORP",
    1:                         "Class": "COM",
    1:                         "Cusip": "594918104",
    1:                         "Value": 1250,
    1:                         "SharesPrnAmount": 10000,
    1:                         "SoleVoting": 9000,
    1:                         "SharedVoting": 500,
    1:                         "NonVoting": 500,
                           }
                       ],
    1:                 other_included_managers_count=2,
    1:                 total_holdings=1,
    1:                 total_value=Decimal("1250"),
    1:                 sections=["Cover page\nThis is a holdings report filed by the manager."],
                   )
               ),
           )
       
    2:     monkeypatch.setattr(
    1:         cli_module,
    1:         "fetch_filings_for_security",
    2:         lambda *, security, route, start_accepted_at: [envelope],
           )
       
    1:     original_extract_field = provider_module.TextExtractionEngine.extract_field
       
    1:     def failing_extract_field(self, *, filing, field_spec):
    1:         if field_spec.field_name == "manager_structure_quant":
    1:             return {"status": "error", "error_code": "FORCED_TEXT_FAILURE"}
>>>>>>         return original_extract_field(self, filing=filing, field_spec=field_spec)
       
    1:     monkeypatch.setattr(provider_module.TextExtractionEngine, "extract_field", failing_extract_field)
       
    1:     repo = FakeRepo()
    1:     security = SimpleNamespace(cik="0001067983", ticker="BRK")
    2:     bundles = cli_module._build_bundles_from_provider(
    1:         security=security,
    1:         route="holding",
    1:         start_accepted_at=datetime(2024, 4, 1, tzinfo=timezone.utc),
    1:         repo=repo,
    1:         run_id="run-holding-text-failure",
           )
       
    1:     assert len(bundles) == 1
    9:     facts = {(fact.field_name, fact.subject_key): fact for fact in bundles[0].facts}
    1:     assert facts[("position_value_usd", "position:1")].value_numeric == 1250000.0
    1:     assert facts[("info_table_value_total_usd", "document")].value_numeric == 1250000.0
    1:     assert ("manager_structure_quant", "document") not in facts
    2:     assert any(log["error_type"] == "FORCED_TEXT_FAILURE" for log in repo.logs)
       
       
    1: def test_build_bundles_from_provider_rejects_invalid_holding_bundle_subject_contract(monkeypatch) -> None:
    2:     envelope = FilingEnvelope(
    1:         accession_no="0000000000-24-000200A",
    1:         cik="0001067983",
    1:         form_type="13F-HR",
    1:         accepted_at=datetime(2024, 5, 15, tzinfo=timezone.utc),
    2:         filing=Fake13FFiling(
    2:             Fake13F(
    1:                 form="13F-HR",
    1:                 rows=[],
    1:                 sections=["Cover page\nThis is a holdings report filed by the manager."],
                   )
               ),
           )
       
    2:     monkeypatch.setattr(
    1:         cli_module,
    1:         "fetch_filings_for_security",
    2:         lambda *, security, route, start_accepted_at: [envelope],
           )
       
    1:     def build_invalid_holding_bundle(*, envelope, filing):
    1:         del envelope
    2:         return BundleBuildOutcome(
    2:             bundle=FilingBundle(
    1:                 filing=filing,
    1:                 facts=[
    2:                     FactInput(
    1:                         field_name="position_value_usd",
    1:                         subject_key="document",
    1:                         value_numeric=1250000.0,
    1:                         confidence=0.99,
                           )
                       ],
    1:                 evidences=[
    2:                     EvidenceInput(
    1:                         field_name="position_value_usd",
    1:                         subject_key="document",
    1:                         locator_kind="obj",
    1:                         source_span="infotable[0].Value",
    1:                         raw_value="1250",
    1:                         normalized_value="1250000.0",
                           )
                       ],
                   )
               )
       
    1:     monkeypatch.setattr(provider_module, "build_holding_13f_bundle", build_invalid_holding_bundle)
       
    1:     repo = FakeRepo()
    1:     security = SimpleNamespace(cik="0001067983", ticker="BRK")
    2:     bundles = cli_module._build_bundles_from_provider(
    1:         security=security,
    1:         route="holding",
    1:         start_accepted_at=datetime(2024, 4, 1, tzinfo=timezone.utc),
    1:         repo=repo,
    1:         run_id="run-holding-invalid-contract",
           )
       
    1:     assert len(bundles) == 1
    2:     text_facts = {(fact.field_name, fact.subject_key): fact.value_text for fact in bundles[0].facts if fact.value_text is not None}
    1:     assert text_facts[("manager_structure_quant", "document")] == "holdings"
    2:     numeric_fields = {(fact.field_name, fact.subject_key) for fact in bundles[0].facts if fact.value_numeric is not None}
    1:     assert ("position_value_usd", "document") not in numeric_fields
    1:     assert repo.logs[-1]["error_type"] == "SPECIALIZED_SUBJECT_CONTRACT_VIOLATION"
    1:     assert "position_value_usd:document" in str(repo.logs[-1]["error_detail"])
       
       
    1: def test_build_bundles_from_provider_preserves_holding_amendment_text_when_bundle_violates_subject_contract(monkeypatch) -> None:
    2:     envelope = FilingEnvelope(
    1:         accession_no="0000000000-24-000201C",
    1:         cik="0001067983",
    1:         form_type="13F-HR/A",
    1:         accepted_at=datetime(2024, 5, 16, tzinfo=timezone.utc),
    2:         filing=Fake13FFiling(
    2:             Fake13F(
    1:                 form="13F-HR/A",
    1:                 rows=[],
    1:                 sections=["Header\nThis filing is a correction of the prior filing."],
                   )
               ),
           )
       
    2:     monkeypatch.setattr(
    1:         cli_module,
    1:         "fetch_filings_for_security",
    2:         lambda *, security, route, start_accepted_at: [envelope],
           )
       
    1:     def invalid_holding_bundle(*, envelope, filing):
    1:         del envelope
    2:         return BundleBuildOutcome(
    2:             bundle=FilingBundle(
    1:                 filing=filing,
    1:                 facts=[
    2:                     FactInput(
    1:                         field_name="position_value_usd",
    1:                         subject_key="document",
    1:                         value_numeric=1250000.0,
    1:                         confidence=0.99,
                           )
                       ],
    1:                 evidences=[
    2:                     EvidenceInput(
    1:                         field_name="position_value_usd",
    1:                         subject_key="document",
    1:                         locator_kind="obj",
    1:                         source_span="infotable[0].Value",
    1:                         raw_value="1250",
    1:                         normalized_value="1250000.0",
                           )
                       ],
                   )
               )
       
    1:     monkeypatch.setattr(provider_module, "build_holding_13f_bundle", invalid_holding_bundle)
       
    1:     repo = FakeRepo()
    1:     security = SimpleNamespace(cik="0001067983", ticker="BRK")
    2:     bundles = cli_module._build_bundles_from_provider(
    1:         security=security,
    1:         route="holding",
    1:         start_accepted_at=datetime(2024, 4, 1, tzinfo=timezone.utc),
    1:         repo=repo,
    1:         run_id="run-holding-amendment-invalid-contract",
           )
       
    1:     assert len(bundles) == 1
    2:     text_facts = {(fact.field_name, fact.subject_key): fact.value_text for fact in bundles[0].facts if fact.value_text is not None}
    1:     assert text_facts[("amendment_scope_quant", "document")] == "correction"
    2:     numeric_fields = {(fact.field_name, fact.subject_key) for fact in bundles[0].facts if fact.value_numeric is not None}
    1:     assert ("position_value_usd", "document") not in numeric_fields
    1:     assert repo.logs[-1]["error_type"] == "SPECIALIZED_SUBJECT_CONTRACT_VIOLATION"
       
       
    1: def test_build_bundles_from_provider_preserves_holding_text_when_bundle_has_no_rows(monkeypatch) -> None:
    2:     envelope = FilingEnvelope(
    1:         accession_no="0000000000-24-000200C",
    1:         cik="0001067983",
    1:         form_type="13F-HR",
    1:         accepted_at=datetime(2024, 5, 15, tzinfo=timezone.utc),
    2:         filing=Fake13FFiling(
    2:             Fake13F(
    1:                 form="13F-HR",
    1:                 rows=[],
    1:                 sections=["Cover page\nThis is a holdings report filed by the manager."],
                   )
               ),
           )
       
    2:     monkeypatch.setattr(
    1:         cli_module,
    1:         "fetch_filings_for_security",
    2:         lambda *, security, route, start_accepted_at: [envelope],
           )
       
    1:     def empty_holding_bundle(*, envelope, filing):
    1:         del envelope
    2:         return BundleBuildOutcome(
    2:             bundle=FilingBundle(
    1:                 filing=filing,
    1:                 facts=[],
    1:                 evidences=[],
                   )
               )
       
    1:     monkeypatch.setattr(provider_module, "build_holding_13f_bundle", empty_holding_bundle)
       
    1:     repo = FakeRepo()
    1:     security = SimpleNamespace(cik="0001067983", ticker="BRK")
    2:     bundles = cli_module._build_bundles_from_provider(
    1:         security=security,
    1:         route="holding",
    1:         start_accepted_at=datetime(2024, 4, 1, tzinfo=timezone.utc),
    1:         repo=repo,
    1:         run_id="run-holding-empty",
           )
       
    1:     assert len(bundles) == 1
    2:     text_facts = {(fact.field_name, fact.subject_key): fact.value_text for fact in bundles[0].facts if fact.value_text is not None}
    1:     assert text_facts[("manager_structure_quant", "document")] == "holdings"
    2:     numeric_fields = {(fact.field_name, fact.subject_key) for fact in bundles[0].facts if fact.value_numeric is not None}
    1:     assert numeric_fields == set()
    1:     assert repo.logs[-1]["error_type"] == "NO_HOLDING_ROWS_EXTRACTED"
       
       
    1: def test_build_bundles_from_provider_preserves_holding_text_when_obj_bundle_unavailable(monkeypatch) -> None:
    2:     envelope = FilingEnvelope(
    1:         accession_no="0000000000-24-000200B",
    1:         cik="0001067983",
    1:         form_type="13F-HR",
    1:         accepted_at=datetime(2024, 5, 15, tzinfo=timezone.utc),
    2:         filing=Fake13FFiling(
    2:             Fake13F(
    1:                 form="13F-HR",
    1:                 rows=[],
    1:                 sections=["Cover page\nThis is a holdings report filed by the manager."],
                   )
               ),
           )
       
    2:     monkeypatch.setattr(
    1:         cli_module,
    1:         "fetch_filings_for_security",
    2:         lambda *, security, route, start_accepted_at: [envelope],
           )
       
    1:     def fail_holding_bundle(*, envelope, filing):
    1:         del envelope, filing
    1:         return BundleBuildOutcome(bundle=None, error_detail="mock 13F obj failure")
       
    1:     monkeypatch.setattr(provider_module, "build_holding_13f_bundle", fail_holding_bundle)
       
    1:     repo = FakeRepo()
    1:     security = SimpleNamespace(cik="0001067983", ticker="BRK")
    2:     bundles = cli_module._build_bundles_from_provider(
    1:         security=security,
    1:         route="holding",
    1:         start_accepted_at=datetime(2024, 4, 1, tzinfo=timezone.utc),
    1:         repo=repo,
    1:         run_id="run-holding-obj-unavailable",
           )
       
    1:     assert len(bundles) == 1
    2:     text_facts = {(fact.field_name, fact.subject_key): fact.value_text for fact in bundles[0].facts if fact.value_text is not None}
    1:     assert text_facts[("manager_structure_quant", "document")] == "holdings"
    2:     numeric_fields = {(fact.field_name, fact.subject_key) for fact in bundles[0].facts if fact.value_numeric is not None}
    1:     assert numeric_fields == set()
    1:     assert repo.logs[-1]["error_type"] == "HOLDING_OBJ_UNAVAILABLE"
       
       
    1: def test_build_bundles_from_provider_preserves_holding_amendment_text_when_obj_bundle_unavailable(monkeypatch) -> None:
    2:     envelope = FilingEnvelope(
    1:         accession_no="0000000000-24-000201A",
    1:         cik="0001067983",
    1:         form_type="13F-HR/A",
    1:         accepted_at=datetime(2024, 5, 16, tzinfo=timezone.utc),
    2:         filing=Fake13FFiling(
    2:             Fake13F(
    1:                 form="13F-HR/A",
    1:                 rows=[],
    1:                 sections=["Header\nThis filing is a correction of the prior filing."],
                   )
               ),
           )
       
    2:     monkeypatch.setattr(
    1:         cli_module,
    1:         "fetch_filings_for_security",
    2:         lambda *, security, route, start_accepted_at: [envelope],
           )
       
    1:     def fail_holding_bundle(*, envelope, filing):
    1:         del envelope, filing
    1:         return BundleBuildOutcome(bundle=None, error_detail="mock 13F amendment obj failure")
       
    1:     monkeypatch.setattr(provider_module, "build_holding_13f_bundle", fail_holding_bundle)
       
    1:     repo = FakeRepo()
    1:     security = SimpleNamespace(cik="0001067983", ticker="BRK")
    2:     bundles = cli_module._build_bundles_from_provider(
    1:         security=security,
    1:         route="holding",
    1:         start_accepted_at=datetime(2024, 4, 1, tzinfo=timezone.utc),
    1:         repo=repo,
    1:         run_id="run-holding-amendment-obj-unavailable",
           )
       
    1:     assert len(bundles) == 1
    2:     text_facts = {(fact.field_name, fact.subject_key): fact.value_text for fact in bundles[0].facts if fact.value_text is not None}
    1:     assert text_facts[("amendment_scope_quant", "document")] == "correction"
    2:     numeric_fields = {(fact.field_name, fact.subject_key) for fact in bundles[0].facts if fact.value_numeric is not None}
    1:     assert numeric_fields == set()
    1:     assert repo.logs[-1]["error_type"] == "HOLDING_OBJ_UNAVAILABLE"
       
       
    1: def test_build_bundles_from_provider_preserves_holding_amendment_text_when_bundle_has_no_rows(monkeypatch) -> None:
    2:     envelope = FilingEnvelope(
    1:         accession_no="0000000000-24-000201B",
    1:         cik="0001067983",
    1:         form_type="13F-HR/A",
    1:         accepted_at=datetime(2024, 5, 16, tzinfo=timezone.utc),
    2:         filing=Fake13FFiling(
    2:             Fake13F(
    1:                 form="13F-HR/A",
    1:                 rows=[],
    1:                 sections=["Header\nThis filing is a correction of the prior filing."],
                   )
               ),
           )
       
    2:     monkeypatch.setattr(
    1:         cli_module,
    1:         "fetch_filings_for_security",
    2:         lambda *, security, route, start_accepted_at: [envelope],
           )
       
    1:     def empty_holding_bundle(*, envelope, filing):
    1:         del envelope
    2:         return BundleBuildOutcome(
    2:             bundle=FilingBundle(
    1:                 filing=filing,
    1:                 facts=[],
    1:                 evidences=[],
                   )
               )
       
    1:     monkeypatch.setattr(provider_module, "build_holding_13f_bundle", empty_holding_bundle)
       
    1:     repo = FakeRepo()
    1:     security = SimpleNamespace(cik="0001067983", ticker="BRK")
    2:     bundles = cli_module._build_bundles_from_provider(
    1:         security=security,
    1:         route="holding",
    1:         start_accepted_at=datetime(2024, 4, 1, tzinfo=timezone.utc),
    1:         repo=repo,
    1:         run_id="run-holding-amendment-empty",
           )
       
    1:     assert len(bundles) == 1
    2:     text_facts = {(fact.field_name, fact.subject_key): fact.value_text for fact in bundles[0].facts if fact.value_text is not None}
    1:     assert text_facts[("amendment_scope_quant", "document")] == "correction"
    2:     numeric_fields = {(fact.field_name, fact.subject_key) for fact in bundles[0].facts if fact.value_numeric is not None}
    1:     assert numeric_fields == set()
    1:     assert repo.logs[-1]["error_type"] == "NO_HOLDING_ROWS_EXTRACTED"
       
       
    1: def test_build_bundles_from_provider_merges_13f_amendment_text(monkeypatch) -> None:
    2:     envelope = FilingEnvelope(
    1:         accession_no="0000000000-24-000201",
    1:         cik="0001067983",
    1:         form_type="13F-HR/A",
    1:         accepted_at=datetime(2024, 5, 16, tzinfo=timezone.utc),
    2:         filing=Fake13FFiling(
    2:             Fake13F(
    1:                 form="13F-HR/A",
    1:                 rows=[
    1:                     {
    1:                         "Issuer": "MICROSOFT CORP",
    1:                         "Class": "COM",
    1:                         "Cusip": "594918104",
    1:                         "Value": 1250,
    1:                         "SharesPrnAmount": 10000,
    1:                         "SoleVoting": 9000,
    1:                         "SharedVoting": 500,
    1:                         "NonVoting": 500,
                           }
                       ],
    1:                 other_included_managers_count=1,
    1:                 total_holdings=1,
    1:                 total_value=Decimal("1250"),
    1:                 sections=["Header\nThis filing is a correction of the prior filing."],
                   )
               ),
           )
       
    2:     monkeypatch.setattr(
    1:         cli_module,
    1:         "fetch_filings_for_security",
    2:         lambda *, security, route, start_accepted_at: [envelope],
           )
       
    1:     repo = FakeRepo()
    1:     security = SimpleNamespace(cik="0001067983", ticker="BRK")
    2:     bundles = cli_module._build_bundles_from_provider(
    1:         security=security,
    1:         route="holding",
    1:         start_accepted_at=datetime(2024, 4, 1, tzinfo=timezone.utc),
    1:         repo=repo,
    1:         run_id="run-holding-2",
           )
       
    1:     assert len(bundles) == 1
    1:     bundle = bundles[0]
   10:     facts = {(fact.field_name, fact.subject_key): fact for fact in bundle.facts}
    1:     assert facts[("position_value_usd", "position:1")].value_numeric == 1250000.0
    1:     assert facts[("info_table_value_total_usd", "document")].value_numeric == 1250000.0
    1:     assert facts[("amendment_scope_quant", "document")].value_text == "correction"
       
   10:     evidence = {(item.field_name, item.subject_key): item for item in bundle.evidences}
    1:     assert evidence[("amendment_scope_quant", "document")].locator_kind == "section_window"
    1:     assert evidence[("position_value_usd", "position:1")].source_span == "infotable[0].Value"
    1:     assert repo.logs == []
       
       
    1: def test_build_bundles_from_provider_keeps_13f_amendment_numeric_when_text_field_extraction_fails(monkeypatch) -> None:
    2:     envelope = FilingEnvelope(
    1:         accession_no="0000000000-24-000201C",
    1:         cik="0001067983",
    1:         form_type="13F-HR/A",
    1:         accepted_at=datetime(2024, 5, 16, tzinfo=timezone.utc),
    2:         filing=Fake13FFiling(
    2:             Fake13F(
    1:                 form="13F-HR/A",
    1:                 rows=[
    1:                     {
    1:                         "Issuer": "MICROSOFT CORP",
    1:                         "Class": "COM",
    1:                         "Cusip": "594918104",
    1:                         "Value": 1250,
    1:                         "SharesPrnAmount": 10000,
    1:                         "SoleVoting": 9000,
    1:                         "SharedVoting": 500,
    1:                         "NonVoting": 500,
                           }
                       ],
    1:                 other_included_managers_count=1,
    1:                 total_holdings=1,
    1:                 total_value=Decimal("1250"),
    1:                 sections=["Header\nThis filing is a correction of the prior filing."],
                   )
               ),
           )
       
    2:     monkeypatch.setattr(
    1:         cli_module,
    1:         "fetch_filings_for_security",
    2:         lambda *, security, route, start_accepted_at: [envelope],
           )
       
    1:     original_extract_field = provider_module.TextExtractionEngine.extract_field
       
    1:     def failing_extract_field(self, *, filing, field_spec):
    1:         if field_spec.field_name == "amendment_scope_quant":
    1:             return {"status": "error", "error_code": "FORCED_TEXT_FAILURE"}
>>>>>>         return original_extract_field(self, filing=filing, field_spec=field_spec)
       
    1:     monkeypatch.setattr(provider_module.TextExtractionEngine, "extract_field", failing_extract_field)
       
    1:     repo = FakeRepo()
    1:     security = SimpleNamespace(cik="0001067983", ticker="BRK")
    2:     bundles = cli_module._build_bundles_from_provider(
    1:         security=security,
    1:         route="holding",
    1:         start_accepted_at=datetime(2024, 4, 1, tzinfo=timezone.utc),
    1:         repo=repo,
    1:         run_id="run-holding-amendment-text-failure",
           )
       
    1:     assert len(bundles) == 1
    9:     facts = {(fact.field_name, fact.subject_key): fact for fact in bundles[0].facts}
    1:     assert facts[("position_value_usd", "position:1")].value_numeric == 1250000.0
    1:     assert facts[("info_table_value_total_usd", "document")].value_numeric == 1250000.0
    1:     assert ("amendment_scope_quant", "document") not in facts
    2:     assert any(log["error_type"] == "FORCED_TEXT_FAILURE" for log in repo.logs)

       # SPDX-License-Identifier: MIT
       # SPDX-FileCopyrightText: 2021 Taneli Hukkinen
       # Licensed to PSF under a Contributor Agreement.
       
    1: from __future__ import annotations
       
    1: from collections.abc import Iterable
    1: import string
    1: from types import MappingProxyType
    1: from typing import Any, BinaryIO, NamedTuple
       
    1: from ._re import (
           RE_DATETIME,
           RE_LOCALTIME,
           RE_NUMBER,
           match_to_datetime,
           match_to_localtime,
           match_to_number,
       )
    1: from ._types import Key, ParseFloat, Pos
       
   34: ASCII_CTRL = frozenset(chr(i) for i in range(32)) | frozenset(chr(127))
       
       # Neither of these sets include quotation mark or backslash. They are
       # currently handled as separate cases in the parser functions.
    1: ILLEGAL_BASIC_STR_CHARS = ASCII_CTRL - frozenset("\t")
    1: ILLEGAL_MULTILINE_BASIC_STR_CHARS = ASCII_CTRL - frozenset("\t\n")
       
    1: ILLEGAL_LITERAL_STR_CHARS = ILLEGAL_BASIC_STR_CHARS
    1: ILLEGAL_MULTILINE_LITERAL_STR_CHARS = ILLEGAL_MULTILINE_BASIC_STR_CHARS
       
    1: ILLEGAL_COMMENT_CHARS = ILLEGAL_BASIC_STR_CHARS
       
    1: TOML_WS = frozenset(" \t")
    1: TOML_WS_AND_NEWLINE = TOML_WS | frozenset("\n")
    1: BARE_KEY_CHARS = frozenset(string.ascii_letters + string.digits + "-_")
    1: KEY_INITIAL_CHARS = BARE_KEY_CHARS | frozenset("\"'")
    1: HEXDIGIT_CHARS = frozenset(string.hexdigits)
       
    2: BASIC_STR_ESCAPE_REPLACEMENTS = MappingProxyType(
    1:     {
    1:         "\\b": "\u0008",  # backspace
    1:         "\\t": "\u0009",  # tab
    1:         "\\n": "\u000A",  # linefeed
    1:         "\\f": "\u000C",  # form feed
    1:         "\\r": "\u000D",  # carriage return
    1:         '\\"': "\u0022",  # quote
    1:         "\\\\": "\u005C",  # backslash
           }
       )
       
       
    2: class TOMLDecodeError(ValueError):
    1:     """An error raised if a document is not valid TOML."""
       
       
    1: def load(fp: BinaryIO, /, *, parse_float: ParseFloat = float) -> dict[str, Any]:
           """Parse TOML from a binary file object."""
>>>>>>     b = fp.read()
>>>>>>     try:
>>>>>>         s = b.decode()
>>>>>>     except AttributeError:
>>>>>>         raise TypeError(
>>>>>>             "File must be opened in binary mode, e.g. use `open('foo.toml', 'rb')`"
>>>>>>         ) from None
>>>>>>     return loads(s, parse_float=parse_float)
       
       
    1: def loads(s: str, /, *, parse_float: ParseFloat = float) -> dict[str, Any]:  # noqa: C901
           """Parse TOML from a string."""
       
           # The spec allows converting "\r\n" to "\n", even in string
           # literals. Let's do so to simplify parsing.
    2:     src = s.replace("\r\n", "\n")
    2:     pos = 0
    2:     out = Output(NestedDict(), Flags())
    2:     header: Key = ()
    2:     parse_float = make_safe_parse_float(parse_float)
       
           # Parse one statement at a time
           # (typically means one line in TOML source)
   48:     while True:
               # 1. Skip line leading whitespace
   48:         pos = skip_chars(src, pos, TOML_WS)
       
               # 2. Parse rules. Expect one of the following:
               #    - end of file
               #    - end of line
               #    - comment
               #    - key/value pair
               #    - append dict to list (and move to its namespace)
               #    - create dict (and move to its namespace)
               # Skip trailing whitespace when applicable.
   48:         try:
   48:             char = src[pos]
    2:         except IndexError:
    2:             break
   46:         if char == "\n":
   10:             pos += 1
   10:             continue
   36:         if char in KEY_INITIAL_CHARS:
   24:             pos = key_value_rule(src, pos, out, header, parse_float)
   24:             pos = skip_chars(src, pos, TOML_WS)
   12:         elif char == "[":
   12:             try:
   12:                 second_char: str | None = src[pos + 1]
>>>>>>             except IndexError:
>>>>>>                 second_char = None
   12:             out.flags.finalize_pending()
   12:             if second_char == "[":
>>>>>>                 pos, header = create_list_rule(src, pos, out)
                   else:
   12:                 pos, header = create_dict_rule(src, pos, out)
   12:             pos = skip_chars(src, pos, TOML_WS)
>>>>>>         elif char != "#":
>>>>>>             raise suffixed_err(src, pos, "Invalid statement")
       
               # 3. Skip comment
   36:         pos = skip_comment(src, pos)
       
               # 4. Expect end of line or end of file
   36:         try:
   36:             char = src[pos]
>>>>>>         except IndexError:
>>>>>>             break
   36:         if char != "\n":
>>>>>>             raise suffixed_err(
>>>>>>                 src, pos, "Expected newline or end of document after a statement"
                   )
   36:         pos += 1
       
    2:     return out.data.dict
       
       
    2: class Flags:
    1:     """Flags that map to parsed keys/namespaces."""
       
           # Marks an immutable namespace (inline array or inline table).
    1:     FROZEN = 0
           # Marks a nest that has been explicitly created and can no longer
           # be opened using the "[table]" syntax.
    1:     EXPLICIT_NEST = 1
       
    1:     def __init__(self) -> None:
    2:         self._flags: dict[str, dict] = {}
    2:         self._pending_flags: set[tuple[Key, int]] = set()
       
    1:     def add_pending(self, key: Key, flag: int) -> None:
>>>>>>         self._pending_flags.add((key, flag))
       
    1:     def finalize_pending(self) -> None:
   12:         for key, flag in self._pending_flags:
>>>>>>             self.set(key, flag, recursive=False)
   12:         self._pending_flags.clear()
       
    1:     def unset_all(self, key: Key) -> None:
>>>>>>         cont = self._flags
>>>>>>         for k in key[:-1]:
>>>>>>             if k not in cont:
>>>>>>                 return
>>>>>>             cont = cont[k]["nested"]
>>>>>>         cont.pop(key[-1], None)
       
    1:     def set(self, key: Key, flag: int, *, recursive: bool) -> None:  # noqa: A003
   22:         cont = self._flags
   22:         key_parent, key_stem = key[:-1], key[-1]
   74:         for k in key_parent:
   52:             if k not in cont:
    8:                 cont[k] = {"flags": set(), "recursive_flags": set(), "nested": {}}
   52:             cont = cont[k]["nested"]
   22:         if key_stem not in cont:
   22:             cont[key_stem] = {"flags": set(), "recursive_flags": set(), "nested": {}}
   22:         cont[key_stem]["recursive_flags" if recursive else "flags"].add(flag)
       
    1:     def is_(self, key: Key, flag: int) -> bool:
   48:         if not key:
>>>>>>             return False  # document root has no flags
   48:         cont = self._flags
  110:         for k in key[:-1]:
   66:             if k not in cont:
    4:                 return False
   62:             inner_cont = cont[k]
   62:             if flag in inner_cont["recursive_flags"]:
>>>>>>                 return True
   62:             cont = inner_cont["nested"]
   44:         key_stem = key[-1]
   44:         if key_stem in cont:
   24:             cont = cont[key_stem]
   24:             return flag in cont["flags"] or flag in cont["recursive_flags"]
   20:         return False
       
       
    2: class NestedDict:
    1:     def __init__(self) -> None:
               # The parsed content of the TOML document
    2:         self.dict: dict[str, Any] = {}
       
    4:     def get_or_create_nest(
               self,
    1:         key: Key,
               *,
    2:         access_lists: bool = True,
    1:     ) -> dict:
   36:         cont: Any = self.dict
  124:         for k in key:
   88:             if k not in cont:
   20:                 cont[k] = {}
   88:             cont = cont[k]
   88:             if access_lists and isinstance(cont, list):
>>>>>>                 cont = cont[-1]
   88:             if not isinstance(cont, dict):
>>>>>>                 raise KeyError("There is no nest behind this key")
   36:         return cont
       
    1:     def append_nest_to_list(self, key: Key) -> None:
>>>>>>         cont = self.get_or_create_nest(key[:-1])
>>>>>>         last_key = key[-1]
>>>>>>         if last_key in cont:
>>>>>>             list_ = cont[last_key]
>>>>>>             if not isinstance(list_, list):
>>>>>>                 raise KeyError("An object other than list found behind this key")
>>>>>>             list_.append({})
               else:
>>>>>>             cont[last_key] = [{}]
       
       
    2: class Output(NamedTuple):
    1:     data: NestedDict
    1:     flags: Flags
       
       
    1: def skip_chars(src: str, pos: Pos, chars: Iterable[str]) -> Pos:
  366:     try:
  992:         while src[pos] in chars:
  626:             pos += 1
    2:     except IndexError:
    2:         pass
  366:     return pos
       
       
    7: def skip_until(
    1:     src: str,
    1:     pos: Pos,
    1:     expect: str,
           *,
    1:     error_on: frozenset[str],
    1:     error_on_eof: bool,
    1: ) -> Pos:
>>>>>>     try:
>>>>>>         new_pos = src.index(expect, pos)
>>>>>>     except ValueError:
>>>>>>         new_pos = len(src)
>>>>>>         if error_on_eof:
>>>>>>             raise suffixed_err(src, new_pos, f"Expected {expect!r}") from None
       
>>>>>>     if not error_on.isdisjoint(src[pos:new_pos]):
>>>>>>         while src[pos] not in error_on:
>>>>>>             pos += 1
>>>>>>         raise suffixed_err(src, pos, f"Found invalid character {src[pos]!r}")
>>>>>>     return new_pos
       
       
    1: def skip_comment(src: str, pos: Pos) -> Pos:
  134:     try:
  134:         char: str | None = src[pos]
>>>>>>     except IndexError:
>>>>>>         char = None
  134:     if char == "#":
>>>>>>         return skip_until(
>>>>>>             src, pos + 1, "\n", error_on=ILLEGAL_COMMENT_CHARS, error_on_eof=False
               )
  134:     return pos
       
       
    1: def skip_comments_and_array_ws(src: str, pos: Pos) -> Pos:
   98:     while True:
   98:         pos_before_skip = pos
   98:         pos = skip_chars(src, pos, TOML_WS_AND_NEWLINE)
   98:         pos = skip_comment(src, pos)
   98:         if pos == pos_before_skip:
   66:             return pos
       
       
    1: def create_dict_rule(src: str, pos: Pos, out: Output) -> tuple[Pos, Key]:
   12:     pos += 1  # Skip "["
   12:     pos = skip_chars(src, pos, TOML_WS)
   12:     pos, key = parse_key(src, pos)
       
   12:     if out.flags.is_(key, Flags.EXPLICIT_NEST) or out.flags.is_(key, Flags.FROZEN):
>>>>>>         raise suffixed_err(src, pos, f"Cannot declare {key} twice")
   12:     out.flags.set(key, Flags.EXPLICIT_NEST, recursive=False)
   12:     try:
   12:         out.data.get_or_create_nest(key)
>>>>>>     except KeyError:
>>>>>>         raise suffixed_err(src, pos, "Cannot overwrite a value") from None
       
   12:     if not src.startswith("]", pos):
>>>>>>         raise suffixed_err(src, pos, "Expected ']' at the end of a table declaration")
   12:     return pos + 1, key
       
       
    1: def create_list_rule(src: str, pos: Pos, out: Output) -> tuple[Pos, Key]:
>>>>>>     pos += 2  # Skip "[["
>>>>>>     pos = skip_chars(src, pos, TOML_WS)
>>>>>>     pos, key = parse_key(src, pos)
       
>>>>>>     if out.flags.is_(key, Flags.FROZEN):
>>>>>>         raise suffixed_err(src, pos, f"Cannot mutate immutable namespace {key}")
           # Free the namespace now that it points to another empty list item...
>>>>>>     out.flags.unset_all(key)
           # ...but this key precisely is still prohibited from table declaration
>>>>>>     out.flags.set(key, Flags.EXPLICIT_NEST, recursive=False)
>>>>>>     try:
>>>>>>         out.data.append_nest_to_list(key)
>>>>>>     except KeyError:
>>>>>>         raise suffixed_err(src, pos, "Cannot overwrite a value") from None
       
>>>>>>     if not src.startswith("]]", pos):
>>>>>>         raise suffixed_err(src, pos, "Expected ']]' at the end of an array declaration")
>>>>>>     return pos + 2, key
       
       
    7: def key_value_rule(
    5:     src: str, pos: Pos, out: Output, header: Key, parse_float: ParseFloat
    1: ) -> Pos:
   24:     pos, key, value = parse_key_value_pair(src, pos, parse_float)
   24:     key_parent, key_stem = key[:-1], key[-1]
   24:     abs_key_parent = header + key_parent
       
   48:     relative_path_cont_keys = (header + key[:i] for i in range(1, len(key)))
   24:     for cont_key in relative_path_cont_keys:
               # Check that dotted key syntax does not redefine an existing table
>>>>>>         if out.flags.is_(cont_key, Flags.EXPLICIT_NEST):
>>>>>>             raise suffixed_err(src, pos, f"Cannot redefine namespace {cont_key}")
               # Containers in the relative path can't be opened with the table syntax or
               # dotted key/value syntax in following table sections.
>>>>>>         out.flags.add_pending(cont_key, Flags.EXPLICIT_NEST)
       
   24:     if out.flags.is_(abs_key_parent, Flags.FROZEN):
>>>>>>         raise suffixed_err(
>>>>>>             src, pos, f"Cannot mutate immutable namespace {abs_key_parent}"
               )
       
   24:     try:
   24:         nest = out.data.get_or_create_nest(abs_key_parent)
>>>>>>     except KeyError:
>>>>>>         raise suffixed_err(src, pos, "Cannot overwrite a value") from None
   24:     if key_stem in nest:
>>>>>>         raise suffixed_err(src, pos, "Cannot overwrite a value")
           # Mark inline table and array namespaces recursively immutable
   24:     if isinstance(value, (dict, list)):
   10:         out.flags.set(header + key, Flags.FROZEN, recursive=True)
   24:     nest[key_stem] = value
   24:     return pos
       
       
    5: def parse_key_value_pair(
    3:     src: str, pos: Pos, parse_float: ParseFloat
    1: ) -> tuple[Pos, Key, Any]:
   24:     pos, key = parse_key(src, pos)
   24:     try:
   24:         char: str | None = src[pos]
>>>>>>     except IndexError:
>>>>>>         char = None
   24:     if char != "=":
>>>>>>         raise suffixed_err(src, pos, "Expected '=' after a key in a key/value pair")
   24:     pos += 1
   24:     pos = skip_chars(src, pos, TOML_WS)
   24:     pos, value = parse_value(src, pos, parse_float)
   24:     return pos, key, value
       
       
    1: def parse_key(src: str, pos: Pos) -> tuple[Pos, Key]:
   36:     pos, key_part = parse_key_part(src, pos)
   36:     key: Key = (key_part,)
   36:     pos = skip_chars(src, pos, TOML_WS)
   62:     while True:
   62:         try:
   62:             char: str | None = src[pos]
>>>>>>         except IndexError:
>>>>>>             char = None
   62:         if char != ".":
   36:             return pos, key
   26:         pos += 1
   26:         pos = skip_chars(src, pos, TOML_WS)
   26:         pos, key_part = parse_key_part(src, pos)
   26:         key += (key_part,)
   26:         pos = skip_chars(src, pos, TOML_WS)
       
       
    1: def parse_key_part(src: str, pos: Pos) -> tuple[Pos, str]:
   62:     try:
   62:         char: str | None = src[pos]
>>>>>>     except IndexError:
>>>>>>         char = None
   62:     if char in BARE_KEY_CHARS:
   60:         start_pos = pos
   60:         pos = skip_chars(src, pos, BARE_KEY_CHARS)
   60:         return pos, src[start_pos:pos]
    2:     if char == "'":
>>>>>>         return parse_literal_str(src, pos)
    2:     if char == '"':
    2:         return parse_one_line_basic_str(src, pos)
>>>>>>     raise suffixed_err(src, pos, "Invalid initial character for a key part")
       
       
    1: def parse_one_line_basic_str(src: str, pos: Pos) -> tuple[Pos, str]:
   46:     pos += 1
   46:     return parse_basic_str(src, pos, multiline=False)
       
       
    1: def parse_array(src: str, pos: Pos, parse_float: ParseFloat) -> tuple[Pos, list]:
   10:     pos += 1
   10:     array: list = []
       
   10:     pos = skip_comments_and_array_ws(src, pos)
   10:     if src.startswith("]", pos):
>>>>>>         return pos + 1, array
   30:     while True:
   30:         pos, val = parse_value(src, pos, parse_float)
   30:         array.append(val)
   30:         pos = skip_comments_and_array_ws(src, pos)
       
   30:         c = src[pos : pos + 1]
   30:         if c == "]":
    4:             return pos + 1, array
   26:         if c != ",":
>>>>>>             raise suffixed_err(src, pos, "Unclosed array")
   26:         pos += 1
       
   26:         pos = skip_comments_and_array_ws(src, pos)
   26:         if src.startswith("]", pos):
    6:             return pos + 1, array
       
       
    1: def parse_inline_table(src: str, pos: Pos, parse_float: ParseFloat) -> tuple[Pos, dict]:
>>>>>>     pos += 1
>>>>>>     nested_dict = NestedDict()
>>>>>>     flags = Flags()
       
>>>>>>     pos = skip_chars(src, pos, TOML_WS)
>>>>>>     if src.startswith("}", pos):
>>>>>>         return pos + 1, nested_dict.dict
>>>>>>     while True:
>>>>>>         pos, key, value = parse_key_value_pair(src, pos, parse_float)
>>>>>>         key_parent, key_stem = key[:-1], key[-1]
>>>>>>         if flags.is_(key, Flags.FROZEN):
>>>>>>             raise suffixed_err(src, pos, f"Cannot mutate immutable namespace {key}")
>>>>>>         try:
>>>>>>             nest = nested_dict.get_or_create_nest(key_parent, access_lists=False)
>>>>>>         except KeyError:
>>>>>>             raise suffixed_err(src, pos, "Cannot overwrite a value") from None
>>>>>>         if key_stem in nest:
>>>>>>             raise suffixed_err(src, pos, f"Duplicate inline table key {key_stem!r}")
>>>>>>         nest[key_stem] = value
>>>>>>         pos = skip_chars(src, pos, TOML_WS)
>>>>>>         c = src[pos : pos + 1]
>>>>>>         if c == "}":
>>>>>>             return pos + 1, nested_dict.dict
>>>>>>         if c != ",":
>>>>>>             raise suffixed_err(src, pos, "Unclosed inline table")
>>>>>>         if isinstance(value, (dict, list)):
>>>>>>             flags.set(key, Flags.FROZEN, recursive=True)
>>>>>>         pos += 1
>>>>>>         pos = skip_chars(src, pos, TOML_WS)
       
       
    5: def parse_basic_str_escape(
    4:     src: str, pos: Pos, *, multiline: bool = False
    1: ) -> tuple[Pos, str]:
>>>>>>     escape_id = src[pos : pos + 2]
>>>>>>     pos += 2
>>>>>>     if multiline and escape_id in {"\\ ", "\\\t", "\\\n"}:
               # Skip whitespace until next non-whitespace character or end of
               # the doc. Error if non-whitespace is found before newline.
>>>>>>         if escape_id != "\\\n":
>>>>>>             pos = skip_chars(src, pos, TOML_WS)
>>>>>>             try:
>>>>>>                 char = src[pos]
>>>>>>             except IndexError:
>>>>>>                 return pos, ""
>>>>>>             if char != "\n":
>>>>>>                 raise suffixed_err(src, pos, "Unescaped '\\' in a string")
>>>>>>             pos += 1
>>>>>>         pos = skip_chars(src, pos, TOML_WS_AND_NEWLINE)
>>>>>>         return pos, ""
>>>>>>     if escape_id == "\\u":
>>>>>>         return parse_hex_char(src, pos, 4)
>>>>>>     if escape_id == "\\U":
>>>>>>         return parse_hex_char(src, pos, 8)
>>>>>>     try:
>>>>>>         return pos, BASIC_STR_ESCAPE_REPLACEMENTS[escape_id]
>>>>>>     except KeyError:
>>>>>>         raise suffixed_err(src, pos, "Unescaped '\\' in a string") from None
       
       
    1: def parse_basic_str_escape_multiline(src: str, pos: Pos) -> tuple[Pos, str]:
>>>>>>     return parse_basic_str_escape(src, pos, multiline=True)
       
       
    1: def parse_hex_char(src: str, pos: Pos, hex_len: int) -> tuple[Pos, str]:
>>>>>>     hex_str = src[pos : pos + hex_len]
>>>>>>     if len(hex_str) != hex_len or not HEXDIGIT_CHARS.issuperset(hex_str):
>>>>>>         raise suffixed_err(src, pos, "Invalid hex value")
>>>>>>     pos += hex_len
>>>>>>     hex_int = int(hex_str, 16)
>>>>>>     if not is_unicode_scalar_value(hex_int):
>>>>>>         raise suffixed_err(src, pos, "Escaped character is not a Unicode scalar value")
>>>>>>     return pos, chr(hex_int)
       
       
    1: def parse_literal_str(src: str, pos: Pos) -> tuple[Pos, str]:
>>>>>>     pos += 1  # Skip starting apostrophe
>>>>>>     start_pos = pos
>>>>>>     pos = skip_until(
>>>>>>         src, pos, "'", error_on=ILLEGAL_LITERAL_STR_CHARS, error_on_eof=True
           )
>>>>>>     return pos + 1, src[start_pos:pos]  # Skip ending apostrophe
       
       
    1: def parse_multiline_str(src: str, pos: Pos, *, literal: bool) -> tuple[Pos, str]:
>>>>>>     pos += 3
>>>>>>     if src.startswith("\n", pos):
>>>>>>         pos += 1
       
>>>>>>     if literal:
>>>>>>         delim = "'"
>>>>>>         end_pos = skip_until(
>>>>>>             src,
>>>>>>             pos,
>>>>>>             "'''",
>>>>>>             error_on=ILLEGAL_MULTILINE_LITERAL_STR_CHARS,
>>>>>>             error_on_eof=True,
               )
>>>>>>         result = src[pos:end_pos]
>>>>>>         pos = end_pos + 3
           else:
>>>>>>         delim = '"'
>>>>>>         pos, result = parse_basic_str(src, pos, multiline=True)
       
           # Add at maximum two extra apostrophes/quotes if the end sequence
           # is 4 or 5 chars long instead of just 3.
>>>>>>     if not src.startswith(delim, pos):
>>>>>>         return pos, result
>>>>>>     pos += 1
>>>>>>     if not src.startswith(delim, pos):
>>>>>>         return pos, result + delim
>>>>>>     pos += 1
>>>>>>     return pos, result + (delim * 2)
       
       
    1: def parse_basic_str(src: str, pos: Pos, *, multiline: bool) -> tuple[Pos, str]:
   46:     if multiline:
>>>>>>         error_on = ILLEGAL_MULTILINE_BASIC_STR_CHARS
>>>>>>         parse_escapes = parse_basic_str_escape_multiline
           else:
   46:         error_on = ILLEGAL_BASIC_STR_CHARS
   46:         parse_escapes = parse_basic_str_escape
   46:     result = ""
   46:     start_pos = pos
  644:     while True:
  644:         try:
  644:             char = src[pos]
>>>>>>         except IndexError:
>>>>>>             raise suffixed_err(src, pos, "Unterminated string") from None
  644:         if char == '"':
   46:             if not multiline:
   46:                 return pos + 1, result + src[start_pos:pos]
>>>>>>             if src.startswith('"""', pos):
>>>>>>                 return pos + 3, result + src[start_pos:pos]
>>>>>>             pos += 1
>>>>>>             continue
  598:         if char == "\\":
>>>>>>             result += src[start_pos:pos]
>>>>>>             pos, parsed_escape = parse_escapes(src, pos)
>>>>>>             result += parsed_escape
>>>>>>             start_pos = pos
>>>>>>             continue
  598:         if char in error_on:
>>>>>>             raise suffixed_err(src, pos, f"Illegal character {char!r}")
  598:         pos += 1
       
       
    5: def parse_value(  # noqa: C901
    3:     src: str, pos: Pos, parse_float: ParseFloat
    1: ) -> tuple[Pos, Any]:
   54:     try:
   54:         char: str | None = src[pos]
>>>>>>     except IndexError:
>>>>>>         char = None
       
           # IMPORTANT: order conditions based on speed of checking and likelihood
       
           # Basic strings
   54:     if char == '"':
   44:         if src.startswith('"""', pos):
>>>>>>             return parse_multiline_str(src, pos, literal=False)
   44:         return parse_one_line_basic_str(src, pos)
       
           # Literal strings
   10:     if char == "'":
>>>>>>         if src.startswith("'''", pos):
>>>>>>             return parse_multiline_str(src, pos, literal=True)
>>>>>>         return parse_literal_str(src, pos)
       
           # Booleans
   10:     if char == "t":
>>>>>>         if src.startswith("true", pos):
>>>>>>             return pos + 4, True
   10:     if char == "f":
>>>>>>         if src.startswith("false", pos):
>>>>>>             return pos + 5, False
       
           # Arrays
   10:     if char == "[":
   10:         return parse_array(src, pos, parse_float)
       
           # Inline tables
>>>>>>     if char == "{":
>>>>>>         return parse_inline_table(src, pos, parse_float)
       
           # Dates and times
>>>>>>     datetime_match = RE_DATETIME.match(src, pos)
>>>>>>     if datetime_match:
>>>>>>         try:
>>>>>>             datetime_obj = match_to_datetime(datetime_match)
>>>>>>         except ValueError as e:
>>>>>>             raise suffixed_err(src, pos, "Invalid date or datetime") from e
>>>>>>         return datetime_match.end(), datetime_obj
>>>>>>     localtime_match = RE_LOCALTIME.match(src, pos)
>>>>>>     if localtime_match:
>>>>>>         return localtime_match.end(), match_to_localtime(localtime_match)
       
           # Integers and "normal" floats.
           # The regex will greedily match any type starting with a decimal
           # char, so needs to be located after handling of dates and times.
>>>>>>     number_match = RE_NUMBER.match(src, pos)
>>>>>>     if number_match:
>>>>>>         return number_match.end(), match_to_number(number_match, parse_float)
       
           # Special floats
>>>>>>     first_three = src[pos : pos + 3]
>>>>>>     if first_three in {"inf", "nan"}:
>>>>>>         return pos + 3, parse_float(first_three)
>>>>>>     first_four = src[pos : pos + 4]
>>>>>>     if first_four in {"-inf", "+inf", "-nan", "+nan"}:
>>>>>>         return pos + 4, parse_float(first_four)
       
>>>>>>     raise suffixed_err(src, pos, "Invalid value")
       
       
    1: def suffixed_err(src: str, pos: Pos, msg: str) -> TOMLDecodeError:
           """Return a `TOMLDecodeError` where error message is suffixed with
           coordinates in source."""
       
>>>>>>     def coord_repr(src: str, pos: Pos) -> str:
>>>>>>         if pos >= len(src):
>>>>>>             return "end of document"
>>>>>>         line = src.count("\n", 0, pos) + 1
>>>>>>         if line == 1:
>>>>>>             column = pos + 1
               else:
>>>>>>             column = pos - src.rindex("\n", 0, pos)
>>>>>>         return f"line {line}, column {column}"
       
>>>>>>     return TOMLDecodeError(f"{msg} (at {coord_repr(src, pos)})")
       
       
    1: def is_unicode_scalar_value(codepoint: int) -> bool:
>>>>>>     return (0 <= codepoint <= 55295) or (57344 <= codepoint <= 1114111)
       
       
    1: def make_safe_parse_float(parse_float: ParseFloat) -> ParseFloat:
           """A decorator to make `parse_float` safe.
       
           `parse_float` must not return dicts or lists, because these types
           would be mixed with parsed TOML tables and arrays, thus confusing
           the parser. The returned decorated callable raises `ValueError`
           instead of returning illegal types.
           """
           # The default `float` callable never returns illegal types. Optimize it.
    2:     if parse_float is float:  # type: ignore[comparison-overlap]
    2:         return float
       
>>>>>>     def safe_parse_float(float_str: str) -> Any:
>>>>>>         float_value = parse_float(float_str)
>>>>>>         if isinstance(float_value, (dict, list)):
>>>>>>             raise ValueError("parse_float must not return dicts or lists")
>>>>>>         return float_value
       
>>>>>>     return safe_parse_float

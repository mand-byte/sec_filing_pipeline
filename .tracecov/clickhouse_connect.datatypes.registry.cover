    1: import logging
       
    1: from typing import Tuple, Dict
    1: from clickhouse_connect.datatypes.base import TypeDef, ClickHouseType, type_map
    1: from clickhouse_connect.driver.exceptions import InternalError
    1: from clickhouse_connect.driver.parser import parse_enum, parse_callable, parse_columns
       
    1: logger = logging.getLogger(__name__)
    1: type_cache: Dict[str, ClickHouseType] = {}
       
       
    1: def parse_name(name: str) -> Tuple[str, str, TypeDef]:
           """
           Converts a ClickHouse type name into the base class and the definition (TypeDef) needed for any
           additional instantiation
           :param name: ClickHouse type name as returned by clickhouse
           :return: The original base name (before arguments), the full name as passed in and the TypeDef object that
            captures any additional arguments
           """
    6:     base = name
    6:     wrappers = []
    6:     keys = tuple()
    6:     if base.startswith('LowCardinality'):
>>>>>>         wrappers.append('LowCardinality')
>>>>>>         base = base[15:-1]
    6:     if base.startswith('Nullable'):
>>>>>>         wrappers.append('Nullable')
>>>>>>         base = base[9:-1]
    6:     if base.startswith('Enum'):
>>>>>>         keys, values = parse_enum(base)
>>>>>>         base = base[:base.find('(')]
    6:     elif base.startswith('Nested'):
>>>>>>         keys, values = parse_columns(base[6:])
>>>>>>         base = 'Nested'
    6:     elif base.startswith('Tuple'):
    1:         keys, values = parse_columns(base[5:])
    1:         base = 'Tuple'
    5:     elif base.startswith('Variant'):
>>>>>>         keys, values = parse_columns(base[7:])
>>>>>>         base = 'Variant'
    5:     elif base.startswith('JSON') and len(base) > 4 and base[4] == '(':
>>>>>>         keys, values = parse_columns(base[4:])
>>>>>>         base = 'JSON'
    5:     elif base == 'Point':
>>>>>>         values = ('Float64', 'Float64')
           else:
    5:         try:
    5:             base, values, _ = parse_callable(base)
>>>>>>         except IndexError:
>>>>>>             raise InternalError(f'Can not parse ClickHouse data type: {name}') from None
    6:     return base, name, TypeDef(tuple(wrappers), keys, values)
       
       
    1: def get_from_name(name: str) -> ClickHouseType:
           """
           Returns the ClickHouseType instance parsed from the ClickHouse type name.  Instances are cached
           :param name: ClickHouse type name as returned by ClickHouse in WithNamesAndTypes FORMAT or the Native protocol
           :return: The instance of the ClickHouse Type
           """
   12:     ch_type = type_cache.get(name, None)
   12:     if not ch_type:
    6:         base, name, type_def = parse_name(name)
    6:         try:
    6:             ch_type = type_map[base].build(type_def)
>>>>>>         except KeyError:
>>>>>>             err_str = f'Unrecognized ClickHouse type base: {base} name: {name}'
>>>>>>             logger.error(err_str)
>>>>>>             raise InternalError(err_str) from None
    6:         type_cache[name] = ch_type
   12:     return ch_type

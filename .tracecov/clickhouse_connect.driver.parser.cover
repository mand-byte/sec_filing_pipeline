    1: from typing import Union, Tuple
       
    1: from clickhouse_connect.driver.common import unescape_identifier
       
       
       # pylint: disable=too-many-branches
    1: def parse_callable(expr) -> Tuple[str, Tuple[Union[str, int], ...], str]:
           """
           Parses a single level ClickHouse optionally 'callable' function/identifier.  The identifier is returned as the
           first value in the response tuple.  If the expression is callable -- i.e. an identifier followed by 0 or more
           arguments in parentheses, the second returned value is a tuple of the comma separated arguments.  The third and
           final tuple value is any text remaining after the initial expression for further parsing/processing.
       
           Examples:
             "Tuple(String, Enum('one' = 1, 'two' = 2))" will return "Tuple", ("String", "Enum('one' = 1,'two' = 2)"), ""
             "MergeTree() PARTITION BY key" will return "MergeTree", (), "PARTITION BY key"
       
           :param expr:  ClickHouse DDL or Column Name expression
           :return: Tuple of the identifier, a tuple of arguments, and remaining text
           """
    5:     expr = expr.strip()
    5:     pos = expr.find('(')
    5:     space = expr.find(' ')
    5:     if pos == -1 and space == -1:
    2:         return expr, (), ''
    3:     if space != -1 and (pos == -1 or space < pos):
>>>>>>         return expr[:space], (), expr[space:].strip()
    3:     name = expr[:pos]
    3:     pos += 1  # Skip first paren
    3:     values = []
    3:     value = ''
    3:     in_str = False
    3:     level = 0
       
    3:     def add_value():
    3:         try:
    3:             values.append(int(value))
    3:         except ValueError:
    3:             values.append(value)
       
   93:     while True:
   93:         char = expr[pos]
   93:         pos += 1
   93:         if in_str:
>>>>>>             value += char
>>>>>>             if char == "'":
>>>>>>                 in_str = False
>>>>>>             elif char == '\\' and expr[pos] == "'" and expr[pos:pos + 4] != "' = " and expr[pos:pos + 2] != "')":
>>>>>>                 value += expr[pos]
>>>>>>                 pos += 1
               else:
   93:             if level == 0:
   21:                 if char == ' ':
>>>>>>                     space = pos
>>>>>>                     temp_char = expr[space]
>>>>>>                     while temp_char == ' ':
>>>>>>                         space += 1
>>>>>>                         temp_char = expr[space]
>>>>>>                     if not value or temp_char in "()',=><0":
>>>>>>                         char = temp_char
>>>>>>                         pos = space + 1
   21:                 if char == ',':
>>>>>>                     add_value()
>>>>>>                     value = ''
>>>>>>                     continue
   21:                 if char == ')':
    3:                     break
   90:             if char == "'" and (not value or 'Enum' in value):
>>>>>>                 in_str = True
   90:             elif char == '(':
    6:                 level += 1
   84:             elif char == ')' and level:
    6:                 level -= 1
   90:             value += char
    3:     if value != '':
    3:         add_value()
    3:     return name, tuple(values), expr[pos:].strip()
       
       
    1: def parse_enum(expr) -> Tuple[Tuple[str], Tuple[int]]:
           """
           Parse a ClickHouse enum definition expression of the form ('key1' = 1, 'key2' = 2)
           :param expr: ClickHouse enum expression/arguments
           :return: Parallel tuples of string enum keys and integer enum values
           """
>>>>>>     keys = []
>>>>>>     values = []
>>>>>>     pos = expr.find('(') + 1
>>>>>>     in_key = False
>>>>>>     key = []
>>>>>>     value = []
>>>>>>     while True:
>>>>>>         char = expr[pos]
>>>>>>         pos += 1
>>>>>>         if in_key:
>>>>>>             if char == "'":
>>>>>>                 keys.append(''.join(key))
>>>>>>                 key = []
>>>>>>                 in_key = False
>>>>>>             elif char == '\\' and expr[pos] == "'" and expr[pos:pos + 4] != "' = " and expr[pos:] != "')":
>>>>>>                 key.append(expr[pos])
>>>>>>                 pos += 1
                   else:
>>>>>>                 key.append(char)
>>>>>>         elif char not in (' ', '='):
>>>>>>             if char == ',':
>>>>>>                 values.append(int(''.join(value)))
>>>>>>                 value = []
>>>>>>             elif char == ')':
>>>>>>                 values.append(int(''.join(value)))
>>>>>>                 break
>>>>>>             elif char == "'" and not value:
>>>>>>                 in_key = True
                   else:
>>>>>>                 value.append(char)
>>>>>>     values, keys = zip(*sorted(zip(values, keys)))
>>>>>>     return tuple(keys), tuple(values)
       
       
    1: def parse_columns(expr: str):
           """
           Parse a ClickHouse column list of the form (col1 String, col2 Array(Tuple(String, Int32))).  This also handles
           unnamed columns (such as Tuple definitions).  Mixed named and unnamed columns are not currently supported.
           :param expr: ClickHouse enum expression/arguments
           :return: Parallel tuples of column types and column types (strings)
           """
    1:     names = []
    1:     columns = []
    1:     pos = 1
    1:     named = False
    1:     level = 0
    1:     label = ''
    1:     quote = None
   17:     while True:
   17:         char = expr[pos]
   17:         pos += 1
   17:         if quote:
>>>>>>             if char == quote:
>>>>>>                 quote = None
>>>>>>             elif char == '\\' and expr[pos] == "'" and expr[pos:pos + 4] != "' = " and expr[pos:pos + 2] != "')":
>>>>>>                 label += expr[pos]
>>>>>>                 pos += 1
               else:
   17:             if level == 0:
   17:                 if char in (' ', '='):
    1:                     if label and not named:
>>>>>>                         names.append(unescape_identifier(label))
>>>>>>                         label = ''
>>>>>>                         named = True
    1:                     char = ''
   16:                 elif char == ',':
    1:                     columns.append(label)
    1:                     named = False
    1:                     label = ''
    1:                     continue
   15:                 elif char == ')':
    1:                     columns.append(label)
    1:                     break
   15:             if char in ("'", '`') and (not label or 'Enum' in label):
>>>>>>                 quote = char
   15:             elif char == '(':
>>>>>>                 level += 1
   15:             elif char == ')':
>>>>>>                 level -= 1
   15:         label += char
    1:     return tuple(names), tuple(columns)

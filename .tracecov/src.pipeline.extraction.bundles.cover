    1: from __future__ import annotations
       
    1: from decimal import Decimal
    1: import math
    1: import re
    1: from typing import Any
    1: import xml.etree.ElementTree as ET
       
    1: import pandas as pd
       
    1: from src.pipeline.route_runtime import BundleBuildOutcome, FilingBundle, _format_exception_detail
    1: from src.pipeline.services import EvidenceInput, FactInput
    1: from src.pipeline.types import FilingRecord
       
       
    1: def coerce_numeric_value(value: object) -> float | None:
  127:     if isinstance(value, bool) or value is None:
>>>>>>         return None
  127:     if isinstance(value, (int, float, Decimal)):
   86:         numeric = float(value)
   86:         return numeric if math.isfinite(numeric) else None
   41:     if isinstance(value, str):
   41:         cleaned = value.strip().replace(",", "")
   41:         if not cleaned:
>>>>>>             return None
   41:         try:
   41:             numeric = float(cleaned)
>>>>>>         except (ValueError, OverflowError):
>>>>>>             return None
   41:         return numeric if math.isfinite(numeric) else None
>>>>>>     return None
       
       
    1: def build_owner_ownership_bundle(*, envelope: Any, filing: FilingRecord) -> BundleBuildOutcome:
    6:     obj_method = getattr(envelope.filing, "obj", None)
    6:     if not callable(obj_method):
>>>>>>         return BundleBuildOutcome(bundle=None)
       
    6:     try:
    6:         ownership_form = obj_method()
>>>>>>     except Exception as exc:
>>>>>>         return BundleBuildOutcome(bundle=None, error_detail=_format_exception_detail(exc))
       
    6:     facts: list[FactInput] = []
    6:     evidences: list[EvidenceInput] = []
       
    6:     transactions = getattr(ownership_form, "transactions", None)
    6:     if transactions is not None:
    6:         if not isinstance(transactions, list):
>>>>>>             try:
>>>>>>                 transactions = list(transactions)
>>>>>>             except Exception:
>>>>>>                 transactions = []
       
    6:         field_specs = (
                   ("shares_acquired_or_disposed", ("shares", "shares_numeric"), "shares"),
                   ("transaction_price_per_share", ("price_per_share", "price_numeric"), "price_per_share"),
                   (
                       "shares_owned_following_txn",
                       ("shares_owned_following_transaction", "shares_owned_following_txn"),
                       "shares_owned_following_transaction",
                   ),
               )
       
   12:         for index, transaction in enumerate(transactions, start=1):
    6:             subject_key = f"txn:{index}"
   24:             for field_name, attr_names, path_name in field_specs:
   18:                 raw_value = None
   18:                 for attr_name in attr_names:
   18:                     raw_value = getattr(transaction, attr_name, None)
   18:                     if raw_value is not None:
   18:                         break
   18:                 numeric_value = coerce_numeric_value(raw_value)
   18:                 if numeric_value is None:
>>>>>>                     continue
   36:                 facts.append(
   36:                     FactInput(
   18:                         field_name=field_name,
   18:                         subject_key=subject_key,
   18:                         value_numeric=numeric_value,
   18:                         confidence=0.99,
                           )
                       )
   36:                 evidences.append(
   36:                     EvidenceInput(
   18:                         field_name=field_name,
   18:                         subject_key=subject_key,
   18:                         locator_kind="obj",
   18:                         source_span=f"transactions[{index - 1}].{path_name}",
   18:                         raw_value=str(raw_value),
   18:                         normalized_value=str(numeric_value),
                           )
                       )
       
    6:     derivative_table = getattr(ownership_form, "derivative_table", None)
    6:     derivative_transactions = getattr(getattr(derivative_table, "transactions", None), "data", None)
    6:     if isinstance(derivative_transactions, pd.DataFrame) and not derivative_transactions.empty:
   10:         for index, row in enumerate(derivative_transactions.itertuples(index=False), start=1):
    6:             subject_key = f"dtxn:{index}"
    6:             derivative_specs = (
    6:                 ("derivative_underlying_shares", getattr(row, "UnderlyingShares", None), "UnderlyingShares"),
    6:                 ("exercise_or_conversion_price", getattr(row, "ExercisePrice", None), "ExercisePrice"),
                   )
   18:             for field_name, raw_value, path_name in derivative_specs:
   12:                 numeric_value = coerce_numeric_value(raw_value)
   12:                 if numeric_value is None:
>>>>>>                     continue
   24:                 facts.append(
   24:                     FactInput(
   12:                         field_name=field_name,
   12:                         subject_key=subject_key,
   12:                         value_numeric=numeric_value,
   12:                         confidence=0.99,
                           )
                       )
   24:                 evidences.append(
   24:                     EvidenceInput(
   12:                         field_name=field_name,
   12:                         subject_key=subject_key,
   12:                         locator_kind="obj",
   12:                         source_span=f"derivative_table.transactions[{index - 1}].{path_name}",
   12:                         raw_value=str(raw_value),
   12:                         normalized_value=str(numeric_value),
                           )
                       )
       
    6:     non_derivative_holdings = getattr(getattr(getattr(ownership_form, "non_derivative_table", None), "holdings", None), "data", None)
    6:     if isinstance(non_derivative_holdings, pd.DataFrame) and not non_derivative_holdings.empty:
    5:         for index, row in enumerate(non_derivative_holdings.itertuples(index=False), start=1):
    3:             numeric_value = coerce_numeric_value(getattr(row, "Shares", None))
    3:             if numeric_value is None:
>>>>>>                 continue
    3:             subject_key = f"nhold:{index}"
    6:             facts.append(
    6:                 FactInput(
    3:                     field_name="non_derivative_shares_owned",
    3:                     subject_key=subject_key,
    3:                     value_numeric=numeric_value,
    3:                     confidence=0.99,
                       )
                   )
    6:             evidences.append(
    6:                 EvidenceInput(
    3:                     field_name="non_derivative_shares_owned",
    3:                     subject_key=subject_key,
    3:                     locator_kind="obj",
    3:                     source_span=f"non_derivative_table.holdings[{index - 1}].Shares",
    3:                     raw_value=str(getattr(row, "Shares", "")),
    3:                     normalized_value=str(numeric_value),
                       )
                   )
       
    6:     derivative_holdings = getattr(getattr(getattr(ownership_form, "derivative_table", None), "holdings", None), "data", None)
    6:     if isinstance(derivative_holdings, pd.DataFrame) and not derivative_holdings.empty:
    5:         for index, row in enumerate(derivative_holdings.itertuples(index=False), start=1):
    3:             subject_key = f"dhold:{index}"
    3:             holding_specs = (
    3:                 ("derivative_underlying_shares", getattr(row, "UnderlyingShares", None), "UnderlyingShares"),
    3:                 ("exercise_or_conversion_price", getattr(row, "ExercisePrice", None), "ExercisePrice"),
                   )
    9:             for field_name, raw_value, path_name in holding_specs:
    6:                 numeric_value = coerce_numeric_value(raw_value)
    6:                 if numeric_value is None:
>>>>>>                     continue
   12:                 facts.append(
   12:                     FactInput(
    6:                         field_name=field_name,
    6:                         subject_key=subject_key,
    6:                         value_numeric=numeric_value,
    6:                         confidence=0.99,
                           )
                       )
   12:                 evidences.append(
   12:                     EvidenceInput(
    6:                         field_name=field_name,
    6:                         subject_key=subject_key,
    6:                         locator_kind="obj",
    6:                         source_span=f"derivative_table.holdings[{index - 1}].{path_name}",
    6:                         raw_value=str(raw_value),
    6:                         normalized_value=str(numeric_value),
                           )
                       )
       
    6:     return BundleBuildOutcome(bundle=FilingBundle(filing=filing, facts=facts, evidences=evidences))
       
       
    1: def build_holding_13f_bundle(*, envelope: Any, filing: FilingRecord) -> BundleBuildOutcome:
    4:     obj_method = getattr(envelope.filing, "obj", None)
    4:     if not callable(obj_method):
>>>>>>         return BundleBuildOutcome(bundle=None)
       
    4:     try:
    4:         holding_report = obj_method()
>>>>>>     except Exception as exc:
>>>>>>         return BundleBuildOutcome(bundle=None, error_detail=_format_exception_detail(exc))
       
    4:     infotable = getattr(holding_report, "infotable", None)
    4:     if not isinstance(infotable, pd.DataFrame):
>>>>>>         return BundleBuildOutcome(bundle=None)
       
    4:     facts: list[FactInput] = []
    4:     evidences: list[EvidenceInput] = []
    4:     row_value_total_usd = 0.0
       
    4:     if not infotable.empty:
    9:         for index, row in enumerate(infotable.itertuples(index=False), start=1):
    5:             subject_key = f"position:{index}"
    5:             position_specs = (
    5:                 ("position_value_usd", getattr(row, "Value", None), "Value", 1000.0),
    5:                 ("shares_or_principal_amount", getattr(row, "SharesPrnAmount", None), "SharesPrnAmount", 1.0),
    5:                 ("sole_voting_auth_shares", getattr(row, "SoleVoting", None), "SoleVoting", 1.0),
    5:                 ("shared_voting_auth_shares", getattr(row, "SharedVoting", None), "SharedVoting", 1.0),
    5:                 ("none_voting_auth_shares", getattr(row, "NonVoting", None), "NonVoting", 1.0),
                   )
   30:             for field_name, raw_value, path_name, scale in position_specs:
   25:                 numeric_value = coerce_numeric_value(raw_value)
   25:                 if numeric_value is None:
>>>>>>                     continue
   25:                 normalized_value = float(numeric_value * scale)
   25:                 if field_name == "position_value_usd":
    5:                     row_value_total_usd += normalized_value
   50:                 facts.append(
   50:                     FactInput(
   25:                         field_name=field_name,
   25:                         subject_key=subject_key,
   25:                         value_numeric=normalized_value,
   25:                         confidence=0.99,
                           )
                       )
   50:                 evidences.append(
   50:                     EvidenceInput(
   25:                         field_name=field_name,
   25:                         subject_key=subject_key,
   25:                         locator_kind="obj",
   25:                         source_span=f"infotable[{index - 1}].{path_name}",
   25:                         raw_value=str(raw_value),
   25:                         normalized_value=str(normalized_value),
                           )
                       )
       
    4:     summary_page = getattr(getattr(holding_report, "primary_form_information", None), "summary_page", None)
    4:     other_included_managers_raw = getattr(summary_page, "other_included_managers_count", None)
    4:     other_included_managers_count = coerce_numeric_value(other_included_managers_raw)
    4:     if other_included_managers_count is not None:
    8:         facts.append(
    8:             FactInput(
    4:                 field_name="other_included_managers_count",
    4:                 subject_key="document",
    4:                 value_numeric=float(other_included_managers_count),
    4:                 confidence=0.99,
                   )
               )
    8:         evidences.append(
    8:             EvidenceInput(
    4:                 field_name="other_included_managers_count",
    4:                 subject_key="document",
    4:                 locator_kind="obj",
    4:                 source_span="summary_page.otherIncludedManagersCount",
    4:                 raw_value=str(other_included_managers_raw),
    4:                 normalized_value=str(float(other_included_managers_count)),
                   )
               )
       
    4:     total_holdings_raw = getattr(holding_report, "total_holdings", None)
    4:     total_holdings = coerce_numeric_value(total_holdings_raw)
    4:     if total_holdings is None:
>>>>>>         total_holdings = float(len(infotable.index))
>>>>>>         total_holdings_raw = len(infotable.index)
>>>>>>         total_holdings_span = "infotable.row_count"
           else:
    4:         total_holdings = float(total_holdings)
    4:         total_holdings_span = "summary_page.tableEntryTotal"
    8:     facts.append(
    8:         FactInput(
    4:             field_name="info_table_entry_total",
    4:             subject_key="document",
    4:             value_numeric=total_holdings,
    4:             confidence=0.99,
               )
           )
    8:     evidences.append(
    8:         EvidenceInput(
    4:             field_name="info_table_entry_total",
    4:             subject_key="document",
    4:             locator_kind="obj",
    4:             source_span=total_holdings_span,
    4:             raw_value=str(total_holdings_raw),
    4:             normalized_value=str(total_holdings),
               )
           )
       
    4:     total_value_raw = getattr(holding_report, "total_value", None)
    4:     total_value = coerce_numeric_value(total_value_raw)
    4:     if total_value is None:
>>>>>>         total_value_usd = row_value_total_usd
>>>>>>         total_value_raw = row_value_total_usd / 1000.0
>>>>>>         total_value_span = "infotable.Value"
           else:
    4:         total_value_usd = float(total_value * 1000.0)
    4:         total_value_span = "summary_page.tableValueTotal"
    8:     facts.append(
    8:         FactInput(
    4:             field_name="info_table_value_total_usd",
    4:             subject_key="document",
    4:             value_numeric=total_value_usd,
    4:             confidence=0.99,
               )
           )
    8:     evidences.append(
    8:         EvidenceInput(
    4:             field_name="info_table_value_total_usd",
    4:             subject_key="document",
    4:             locator_kind="obj",
    4:             source_span=total_value_span,
    4:             raw_value=str(total_value_raw),
    4:             normalized_value=str(total_value_usd),
               )
           )
       
    4:     return BundleBuildOutcome(bundle=FilingBundle(filing=filing, facts=facts, evidences=evidences))
       
       
    1: def build_issuer_8k_vote_bundle(*, envelope: Any, filing: FilingRecord) -> BundleBuildOutcome:
>>>>>>     obj_method = getattr(envelope.filing, "obj", None)
>>>>>>     if not callable(obj_method):
>>>>>>         return BundleBuildOutcome(bundle=None)
       
>>>>>>     try:
>>>>>>         report = obj_method()
>>>>>>     except Exception as exc:
>>>>>>         return BundleBuildOutcome(bundle=None, error_detail=_format_exception_detail(exc))
       
>>>>>>     items = getattr(report, "items", None)
>>>>>>     item_key: str | None = None
>>>>>>     if isinstance(items, list):
>>>>>>         for candidate in items:
>>>>>>             candidate_text = str(candidate)
>>>>>>             normalized_candidate = candidate_text.upper().replace(" ", "")
>>>>>>             if normalized_candidate in {"ITEM5.07", "5.07", "ITEM5.07."}:
>>>>>>                 item_key = candidate_text
>>>>>>                 break
>>>>>>             if "5.07" in normalized_candidate:
>>>>>>                 item_key = candidate_text
>>>>>>                 break
       
>>>>>>     item_present = item_key is not None
>>>>>>     if not item_present:
>>>>>>         return BundleBuildOutcome(
>>>>>>             bundle=FilingBundle(filing=filing, facts=[], evidences=[]),
>>>>>>             item_present=False,
               )
       
>>>>>>     try:
>>>>>>         item_text = report[item_key]
>>>>>>     except Exception as exc:
>>>>>>         return BundleBuildOutcome(
>>>>>>             bundle=FilingBundle(filing=filing, facts=[], evidences=[]),
>>>>>>             error_detail=_format_exception_detail(exc),
>>>>>>             item_present=True,
               )
       
>>>>>>     if not isinstance(item_text, str) or not item_text.strip():
>>>>>>         return BundleBuildOutcome(
>>>>>>             bundle=FilingBundle(filing=filing, facts=[], evidences=[]),
>>>>>>             item_present=True,
               )
       
>>>>>>     facts: list[FactInput] = []
>>>>>>     evidences: list[EvidenceInput] = []
>>>>>>     lines = [line.strip() for line in item_text.splitlines() if line.strip()]
>>>>>>     proposal_index = 0
       
>>>>>>     for line_number, line in enumerate(lines, start=1):
>>>>>>         all_numeric_tokens = [
>>>>>>             match.group(0)
>>>>>>             for match in re.finditer(r"\d[\d,]*", line)
               ]
>>>>>>         if len(all_numeric_tokens) < 3:
>>>>>>             continue
       
>>>>>>         selected_token_values: list[str] | None = None
>>>>>>         for count in (4, 3):
>>>>>>             if len(all_numeric_tokens) < count:
>>>>>>                 continue
>>>>>>             candidate_tokens = all_numeric_tokens[-count:]
>>>>>>             token_pattern = r"\s+".join(re.escape(token) for token in candidate_tokens)
>>>>>>             trailing_vote_match = re.match(
>>>>>>                 rf"^(?P<label>.+?)\s+(?P<tail>{token_pattern})\s*$",
>>>>>>                 line,
                   )
>>>>>>             if trailing_vote_match is None:
>>>>>>                 continue
>>>>>>             label = trailing_vote_match.group("label").strip()
>>>>>>             if not label or not re.search(r"[A-Za-z]", label):
>>>>>>                 continue
>>>>>>             selected_token_values = candidate_tokens
>>>>>>             break
       
>>>>>>         if selected_token_values is None:
>>>>>>             continue
       
>>>>>>         selected_numeric_values: list[float] = []
>>>>>>         for token in selected_token_values:
>>>>>>             numeric_value = coerce_numeric_value(token)
>>>>>>             if numeric_value is None:
>>>>>>                 selected_numeric_values = []
>>>>>>                 break
>>>>>>             selected_numeric_values.append(float(numeric_value))
       
>>>>>>         if len(selected_numeric_values) < 3:
>>>>>>             continue
       
>>>>>>         proposal_index += 1
>>>>>>         subject_key = f"proposal:{proposal_index}"
>>>>>>         field_values = [
>>>>>>             ("proposal_votes_for", selected_numeric_values[0]),
>>>>>>             ("proposal_votes_against", selected_numeric_values[1]),
>>>>>>             ("proposal_votes_abstain", selected_numeric_values[2]),
               ]
>>>>>>         if len(selected_numeric_values) >= 4:
>>>>>>             field_values.append(("proposal_broker_non_votes", selected_numeric_values[3]))
       
>>>>>>         for field_name, numeric_value in field_values:
>>>>>>             facts.append(
>>>>>>                 FactInput(
>>>>>>                     field_name=field_name,
>>>>>>                     subject_key=subject_key,
>>>>>>                     value_numeric=numeric_value,
>>>>>>                     confidence=0.99,
                       )
                   )
>>>>>>             evidences.append(
>>>>>>                 EvidenceInput(
>>>>>>                     field_name=field_name,
>>>>>>                     subject_key=subject_key,
>>>>>>                     locator_kind="obj",
>>>>>>                     source_span=f"items[{item_key}].line[{line_number}]",
>>>>>>                     source_item_no="5.07",
>>>>>>                     raw_value=line,
>>>>>>                     normalized_value=str(numeric_value),
                       )
                   )
       
>>>>>>     return BundleBuildOutcome(
>>>>>>         bundle=FilingBundle(filing=filing, facts=facts, evidences=evidences),
>>>>>>         item_present=item_present,
           )
       
       
    1: def strip_namespace(tag: str) -> str:
  287:     if "}" in tag:
>>>>>>         return tag.rsplit("}", 1)[1]
  287:     return tag
       
       
    1: def xml_root(xml_text: str) -> ET.Element | None:
    5:     try:
    5:         return ET.fromstring(xml_text)
>>>>>>     except ET.ParseError:
>>>>>>         return None
       
       
    1: def xml_findall(element: ET.Element, tag_name: str) -> list[ET.Element]:
   58:     return [child for child in element.iter() if strip_namespace(child.tag) == tag_name]
       
       
    1: def xml_child_text(element: ET.Element, tag_name: str) -> str | None:
  194:     for child in element.iter():
  193:         if strip_namespace(child.tag) != tag_name:
  154:             continue
   39:         if child.text is None:
>>>>>>             continue
   39:         text = child.text.strip()
   39:         if text:
   39:             return text
    1:     return None
       
       
    1: def xml_direct_child_text(element: ET.Element, tag_name: str) -> str | None:
    2:     for child in list(element):
    2:         if strip_namespace(child.tag) != tag_name:
>>>>>>             continue
    2:         if child.text is None:
>>>>>>             continue
    2:         text = child.text.strip()
    2:         if text:
    2:             return text
>>>>>>     return None
       
       
    1: def extract_monetary_amount(text: str | None) -> float | None:
    6:     if not isinstance(text, str):
    1:         return None
       
    5:     normalized_text = " ".join(text.split())
    5:     anchored_currency_patterns = (
               r"(?i)(?:total\s+funds\s+used|aggregate\s+purchase\s+price|purchase\s+price|source\s+and\s+amount\s+of\s+funds|reporting\s+person\s+used)\D{0,32}\$\s*([-+]?\d[\d,]*(?:\.\d+)?)",
               r"(?i)\$\s*([-+]?\d[\d,]*(?:\.\d+)?)\D{0,32}(?:in\s+cash|of\s+working\s+capital|from\s+working\s+capital|aggregate\s+purchase\s+price|purchase\s+price)",
           )
    6:     for pattern in anchored_currency_patterns:
    6:         match = re.search(pattern, normalized_text)
    6:         if match is None:
    1:             continue
    5:         numeric_value = coerce_numeric_value(match.group(1))
    5:         if numeric_value is not None:
    5:             return float(numeric_value)
       
>>>>>>     currency_matches = [
>>>>>>         coerce_numeric_value(match.group(1))
>>>>>>         for match in re.finditer(r"\$\s*([-+]?\d[\d,]*(?:\.\d+)?)", normalized_text)
           ]
>>>>>>     currency_candidates = [value for value in currency_matches if value is not None]
>>>>>>     if len(currency_candidates) == 1:
>>>>>>         return float(currency_candidates[0])
>>>>>>     if len(currency_candidates) > 1:
>>>>>>         return None
       
>>>>>>     anchored_plain_patterns = (
               r"(?i)(?:total\s+funds\s+used|aggregate\s+purchase\s+price|purchase\s+price|reporting\s+person\s+used)\D{0,16}([-+]?\d{1,3}(?:,\d{3})+|[-+]?\d{5,}(?:\.\d+)?)",
           )
>>>>>>     for pattern in anchored_plain_patterns:
>>>>>>         match = re.search(pattern, normalized_text)
>>>>>>         if match is None:
>>>>>>             continue
>>>>>>         numeric_value = coerce_numeric_value(match.group(1))
>>>>>>         if numeric_value is not None:
>>>>>>             return float(numeric_value)
       
>>>>>>     return None
       
       
    1: def build_owner_schedule_13dg_bundle(*, envelope: Any, filing: FilingRecord, form_family: str) -> BundleBuildOutcome:
    5:     xml_method = getattr(envelope.filing, "xml", None)
    5:     if not callable(xml_method):
>>>>>>         return BundleBuildOutcome(bundle=None)
       
    5:     try:
    5:         xml_text = xml_method()
>>>>>>     except Exception as exc:
>>>>>>         return BundleBuildOutcome(bundle=None, error_detail=_format_exception_detail(exc))
       
    5:     if not isinstance(xml_text, str) or not xml_text.strip():
>>>>>>         return BundleBuildOutcome(bundle=None)
       
    5:     root = xml_root(xml_text)
    5:     if root is None:
>>>>>>         return BundleBuildOutcome(bundle=None)
       
    5:     if form_family == "13G":
    2:         person_blocks = xml_findall(root, "coverPageHeaderReportingPersonDetails")
    2:         field_map = (
                   ("beneficially_owned_shares", "reportingPersonBeneficiallyOwnedAggregateNumberOfShares"),
                   ("beneficial_ownership_pct", "classPercent"),
                   ("sole_voting_power", "soleVotingPower"),
                   ("shared_voting_power", "sharedVotingPower"),
                   ("sole_dispositive_power", "soleDispositivePower"),
                   ("shared_dispositive_power", "sharedDispositivePower"),
               )
           else:
    3:         person_blocks = xml_findall(root, "reportingPerson")
    3:         if not person_blocks:
>>>>>>             person_blocks = xml_findall(root, "coverPageReportingPerson")
    3:         field_map = (
                   ("beneficially_owned_shares", "aggregateAmountOwned"),
                   ("beneficial_ownership_pct", "percentOfClass"),
                   ("sole_voting_power", "soleVotingPower"),
                   ("shared_voting_power", "sharedVotingPower"),
                   ("sole_dispositive_power", "soleDispositivePower"),
                   ("shared_dispositive_power", "sharedDispositivePower"),
               )
       
    5:     facts: list[FactInput] = []
    5:     evidences: list[EvidenceInput] = []
    5:     emitted_source_of_funds_subjects: set[str] = set()
    5:     emitted_aggregate_purchase_subjects: set[str] = set()
       
   35:     def append_13d_funds_amount_fact(
               *,
    5:         field_name: str,
    5:         subject_key: str,
    5:         source_span: str,
    5:         raw_value: str | None,
    5:         numeric_value: float,
    5:     ) -> None:
   16:         facts.append(
   16:             FactInput(
    8:                 field_name=field_name,
    8:                 subject_key=subject_key,
    8:                 value_numeric=float(numeric_value),
    8:                 confidence=0.99,
                   )
               )
   16:         evidences.append(
   16:             EvidenceInput(
    8:                 field_name=field_name,
    8:                 subject_key=subject_key,
    8:                 locator_kind="obj",
    8:                 source_span=source_span,
    8:                 raw_value=str(raw_value),
    8:                 normalized_value=str(float(numeric_value)),
                   )
               )
       
   11:     for index, block in enumerate(person_blocks, start=1):
    6:         subject_key = f"filer:{index}"
   42:         for field_name, tag_name in field_map:
   36:             raw_value = xml_child_text(block, tag_name)
   36:             numeric_value = coerce_numeric_value(raw_value)
   36:             if numeric_value is None:
>>>>>>                 continue
   72:             facts.append(
   72:                 FactInput(
   36:                     field_name=field_name,
   36:                     subject_key=subject_key,
   36:                     value_numeric=float(numeric_value),
   36:                     confidence=0.99,
                       )
                   )
   72:             evidences.append(
   72:                 EvidenceInput(
   36:                     field_name=field_name,
   36:                     subject_key=subject_key,
   36:                     locator_kind="obj",
   36:                     source_span=f"xml.{strip_namespace(block.tag)}[{index - 1}].{tag_name}",
   36:                     raw_value=str(raw_value),
   36:                     normalized_value=str(float(numeric_value)),
                       )
                   )
       
    6:         if form_family != "13D":
    2:             continue
       
    4:         funds_source_text = xml_child_text(block, "fundsSource")
    4:         funds_amount = extract_monetary_amount(funds_source_text)
    4:         if funds_amount is not None:
    3:             source_span = f"xml.{strip_namespace(block.tag)}[{index - 1}].fundsSource"
    6:             append_13d_funds_amount_fact(
    3:                 field_name="source_of_funds_amount",
    3:                 subject_key=subject_key,
    3:                 source_span=source_span,
    3:                 raw_value=funds_source_text,
    3:                 numeric_value=float(funds_amount),
                   )
    3:             emitted_source_of_funds_subjects.add(subject_key)
    6:             append_13d_funds_amount_fact(
    3:                 field_name="aggregate_purchase_price",
    3:                 subject_key=subject_key,
    3:                 source_span=source_span,
    3:                 raw_value=funds_source_text,
    3:                 numeric_value=float(funds_amount),
                   )
    3:             emitted_aggregate_purchase_subjects.add(subject_key)
       
    5:     if form_family == "13D" and len(person_blocks) == 1:
    2:         subject_key = "filer:1"
    2:         funds_source_text = xml_direct_child_text(root, "fundsSource")
    2:         funds_amount = extract_monetary_amount(funds_source_text)
    2:         if funds_amount is not None:
    2:             if subject_key not in emitted_source_of_funds_subjects:
    2:                 append_13d_funds_amount_fact(
    1:                     field_name="source_of_funds_amount",
    1:                     subject_key=subject_key,
    1:                     source_span="xml.fundsSource",
    1:                     raw_value=funds_source_text,
    1:                     numeric_value=float(funds_amount),
                       )
    2:             if subject_key not in emitted_aggregate_purchase_subjects:
    2:                 append_13d_funds_amount_fact(
    1:                     field_name="aggregate_purchase_price",
    1:                     subject_key=subject_key,
    1:                     source_span="xml.fundsSource",
    1:                     raw_value=funds_source_text,
    1:                     numeric_value=float(funds_amount),
                       )
       
    5:     return BundleBuildOutcome(bundle=FilingBundle(filing=filing, facts=facts, evidences=evidences))
       
       
    1: def build_owner_form144_bundle(*, envelope: Any, filing: FilingRecord) -> BundleBuildOutcome:
    3:     obj_method = getattr(envelope.filing, "obj", None)
    3:     if not callable(obj_method):
>>>>>>         return BundleBuildOutcome(bundle=None)
       
    3:     try:
    3:         form144 = obj_method()
>>>>>>     except Exception as exc:
>>>>>>         return BundleBuildOutcome(bundle=None, error_detail=_format_exception_detail(exc))
       
    3:     facts: list[FactInput] = []
    3:     evidences: list[EvidenceInput] = []
       
    3:     securities_information = getattr(form144, "securities_information", None)
    3:     if isinstance(securities_information, pd.DataFrame) and not securities_information.empty:
    4:         for index, row in enumerate(securities_information.itertuples(index=False), start=1):
    2:             subject_key = f"sale_notice:{index}"
    2:             sale_specs = (
    2:                 ("proposed_sale_shares", getattr(row, "units_to_be_sold", None), "securities_information.units_to_be_sold"),
    2:                 ("proposed_sale_market_value", getattr(row, "market_value", None), "securities_information.market_value"),
                   )
    6:             for field_name, raw_value, path_name in sale_specs:
    4:                 numeric_value = coerce_numeric_value(raw_value)
    4:                 if numeric_value is None:
>>>>>>                     continue
    8:                 facts.append(
    8:                     FactInput(
    4:                         field_name=field_name,
    4:                         subject_key=subject_key,
    4:                         value_numeric=float(numeric_value),
    4:                         confidence=0.99,
                           )
                       )
    8:                 evidences.append(
    8:                     EvidenceInput(
    4:                         field_name=field_name,
    4:                         subject_key=subject_key,
    4:                         locator_kind="obj",
    4:                         source_span=f"{path_name}[{index - 1}]",
    4:                         raw_value=str(raw_value),
    4:                         normalized_value=str(float(numeric_value)),
                           )
                       )
       
    3:     securities_sold = getattr(form144, "securities_sold_past_3_months", None)
    3:     if isinstance(securities_sold, pd.DataFrame) and not securities_sold.empty:
    6:         for index, row in enumerate(securities_sold.itertuples(index=False), start=1):
    3:             subject_key = f"sold_past_3m:{index}"
    3:             sold_specs = (
    3:                 ("shares_sold_past_3m", getattr(row, "amount_sold", None), "securities_sold_past_3_months.amount_sold"),
    3:                 ("market_value_sold_past_3m", getattr(row, "gross_proceeds", None), "securities_sold_past_3_months.gross_proceeds"),
                   )
    9:             for field_name, raw_value, path_name in sold_specs:
    6:                 numeric_value = coerce_numeric_value(raw_value)
    6:                 if numeric_value is None:
>>>>>>                     continue
   12:                 facts.append(
   12:                     FactInput(
    6:                         field_name=field_name,
    6:                         subject_key=subject_key,
    6:                         value_numeric=float(numeric_value),
    6:                         confidence=0.99,
                           )
                       )
   12:                 evidences.append(
   12:                     EvidenceInput(
    6:                         field_name=field_name,
    6:                         subject_key=subject_key,
    6:                         locator_kind="obj",
    6:                         source_span=f"{path_name}[{index - 1}]",
    6:                         raw_value=str(raw_value),
    6:                         normalized_value=str(float(numeric_value)),
                           )
                       )
       
    3:     return BundleBuildOutcome(bundle=FilingBundle(filing=filing, facts=facts, evidences=evidences))

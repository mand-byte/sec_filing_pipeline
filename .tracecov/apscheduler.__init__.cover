    1: import importlib.metadata as importlib_metadata
    1: import sys
       
    1: try:
    1:     release = importlib_metadata.version("APScheduler").split("-")[0]
>>>>>> except importlib_metadata.PackageNotFoundError:
>>>>>>     release = "3.5.0"
       
    5: version_info = tuple(int(x) if x.isdigit() else x for x in release.split("."))
    5: version = __version__ = ".".join(str(x) for x in version_info[:3])
    1: del sys, importlib_metadata

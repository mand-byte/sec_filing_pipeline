# SEC Filing Backend - Phase 1

This repository contains the SEC filing pipeline foundation for Phase 0 and Phase 1, with focus on deterministic ingestion, replayable raw artifacts, and owner-route processing.

## Development

```bash
uv sync --dev
uv run pytest -v
```
